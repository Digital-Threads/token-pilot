{"id":"TP-6fo","title":"Claude Code marketplace plugin submission","description":"Register token-pilot as a first-class Claude Code plugin so users can install with one command (/plugin install token-pilot) instead of the npx + install-agents two-step. Bundle already exists in .claude-plugin/: plugin.json, hooks/hooks.json (Read + Edit + SessionStart), skills/ via ../skills reference, and dist/agents/*.md shipped in the npm tarball. Required steps: finalise plugin.json metadata, submit to anthropics/claude-plugins-official or publish our own marketplace.json, verify auto-load works for agents + skills + hooks without needing token-pilot init. This also finally makes skills reachable for the majority of users.","status":"open","priority":1,"issue_type":"feature","owner":"shahinyanm@gmail.com","created_at":"2026-04-18T07:55:08Z","created_by":"shahinyanm","updated_at":"2026-04-18T07:55:08Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"TP-02l","title":"Tier 2 tp-* subagent pack (5 agents)","description":"Ship five Tier 2 agents predicted in TP-816 section 5.4.2, following the Tier 1 pattern (shared preamble + role block + response contract, frontmatter with mcp__token-pilot__* tools, response budget): tp-dead-code-hunter (find_unused + find_usages; budget 400), tp-api-surveyor (module_info + outline + find_usages; 500), tp-history-explorer (smart_log + smart_diff; 500), tp-audit-scanner (code_audit + find_usages; 500), tp-session-restorer (session_snapshot; 300). Each goes through prompt-contract tests + fixture-agent compat like Tier 1. Rolling out agent-by-agent is fine — telemetry-gated per TP-816 section 5.4.7.","status":"closed","priority":1,"issue_type":"feature","owner":"shahinyanm@gmail.com","created_at":"2026-04-18T07:55:02Z","created_by":"shahinyanm","updated_at":"2026-04-18T09:58:02Z","closed_at":"2026-04-18T09:58:02Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"TP-jzh","title":"Bash output truncator (PostToolUse hook for Bash tool)","description":"New PostToolUse hook on the Bash tool, separate from the Read hook. When Bash output exceeds a configurable size (default ~200 lines or ~8000 chars), replace body with head 40 + tail 20 lines + note + path to full output in .token-pilot/bash-output/\u003cts\u003e.log. Covers npm test, git log --no-pager, find, grep with many matches. Respects TOKEN_PILOT_BYPASS. Opt-out via config hooks.truncateBashOutput=false. Expected savings: very high on test-heavy and exploration sessions (tests alone can dump 10k+ lines of irrelevant passing-test noise).","status":"closed","priority":1,"issue_type":"feature","owner":"shahinyanm@gmail.com","created_at":"2026-04-18T07:54:39Z","created_by":"shahinyanm","updated_at":"2026-04-18T08:15:02Z","closed_at":"2026-04-18T08:15:02Z","close_reason":"Shipped in v0.21.0. TP-c08: doctor advisor surfaces CLAUDE_CODE_SUBAGENT_MODEL/MAX_THINKING_TOKENS/CLAUDE_AUTOCOMPACT_PCT_OVERRIDE/model=sonnet tips. TP-rtg: .claudeignore generator (managed-marker, non-destructive) + CLAUDE.md hygiene check in doctor. TP-jzh: re-framed as Bash output advisor after Context7 confirmed PostToolUse cannot truncate tool_response for non-MCP Bash tool — now a PostToolUse:Bash hook that appends a one-line tip when output exceeds ~8000 chars. 843 tests, tsc clean. Release https://github.com/Digital-Threads/token-pilot/releases/tag/v0.21.0","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"TP-rtg","title":".claudeignore generator + CLAUDE.md hygiene doctor check","description":"Two one-time wins that compound every message. (a) On token-pilot init, offer to create .claudeignore with sensible defaults (node_modules, dist, lockfiles, caches, generated code). Ecosystem-standard file, works for other tools too. Merge with our config.ignore. (b) token-pilot doctor warns when project CLAUDE.md exceeds 60 non-empty lines — that file loads into EVERY Claude Code message, so a 200-line CLAUDE.md costs thousands of tokens per turn. Follow-up (separate PR): interactive split-assistant that suggests docs/ fragments to load on demand.","status":"closed","priority":1,"issue_type":"feature","owner":"shahinyanm@gmail.com","created_at":"2026-04-18T07:54:33Z","created_by":"shahinyanm","updated_at":"2026-04-18T08:15:02Z","closed_at":"2026-04-18T08:15:02Z","close_reason":"Shipped in v0.21.0. TP-c08: doctor advisor surfaces CLAUDE_CODE_SUBAGENT_MODEL/MAX_THINKING_TOKENS/CLAUDE_AUTOCOMPACT_PCT_OVERRIDE/model=sonnet tips. TP-rtg: .claudeignore generator (managed-marker, non-destructive) + CLAUDE.md hygiene check in doctor. TP-jzh: re-framed as Bash output advisor after Context7 confirmed PostToolUse cannot truncate tool_response for non-MCP Bash tool — now a PostToolUse:Bash hook that appends a one-line tip when output exceeds ~8000 chars. 843 tests, tsc clean. Release https://github.com/Digital-Threads/token-pilot/releases/tag/v0.21.0","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"TP-69m","title":"Session-scoped dedup: seen-in-session marker + diff-since-last-read + grouped find_usages + Write-invalidation","description":"Extract more savings from what the hook already intercepts. Four tightly-related mechanics: (a) when the same file+session+content-hash was summarised before, return a 1-line pointer 'already seen this session; use read_symbol for specifics', not a new summary; (b) if hash differs since last time, return only read_diff since prior; (c) find_usages groups identical signatures across N files into one line instead of N blocks; (d) PostToolUse on Edit/Write invalidates our file cache + project_overview cache so next read is fresh. Must survive /clear (new session_id → clean slate). Benchmark target: additional 20-30 percent saving on the hook path.","status":"closed","priority":1,"issue_type":"feature","owner":"shahinyanm@gmail.com","created_at":"2026-04-18T07:54:28Z","created_by":"shahinyanm","updated_at":"2026-04-18T09:58:07Z","closed_at":"2026-04-18T09:58:07Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"TP-gsk","title":"v0.21 — enhanced savings, extended family, distribution","description":"Umbrella for v0.21 work. Three goals: (1) extract more savings from what we already intercept via session-scoped dedup, .claudeignore + CLAUDE.md hygiene, and Bash output truncation; (2) extend tp-* family with Tier 2 agents + self-regulation tools so delegates can shape their own responses; (3) broaden distribution via Claude Code marketplace submission.\n\nSUB-ISSUES (bd doesn't support parent-of link between epic and tasks; list here instead):\n\nMust-have (5):\n  TP-69m — Session-scoped dedup block (seen marker + diff-since-last + grouped find_usages + Write-invalidation)\n  TP-rtg — .claudeignore generator + CLAUDE.md hygiene doctor check\n  TP-jzh — Bash output truncator (PostToolUse hook)\n  TP-02l — Tier 2 tp-* subagent pack (5 agents: dead-code-hunter, api-surveyor, history-explorer, audit-scanner, session-restorer)\n  TP-6fo — Claude Code marketplace plugin submission\n\nShould-have (3):\n  TP-hsz — Agent self-regulation tools (budget MCP + time-to-compact + project_overview delta + auto-suggest context-mode)\n  TP-q33 — Enforce per-agent response budgets + live behavioural test\n  TP-c08 — doctor: detect Claude Code env vars from community guide\n\nNice-to-have (4):\n  TP-bbo — Adaptive threshold\n  TP-340 — Session continuity (auto-snapshot + auto-restore)\n  TP-89n — save-doc helper\n  TP-7i3 — Hook daemon (cold-start removal)","status":"closed","priority":1,"issue_type":"epic","owner":"shahinyanm@gmail.com","created_at":"2026-04-18T07:53:49Z","created_by":"shahinyanm","updated_at":"2026-04-18T09:59:12Z","closed_at":"2026-04-18T09:59:12Z","close_reason":"Epic delivered across v0.21.0 (TP-c08, TP-rtg, TP-jzh), v0.21.1 (TP-bbo, TP-89n), v0.21.2 (TP-340, TP-hsz), v0.22.0 (TP-69m), v0.22.1 (TP-02l), v0.22.2 (review hot-fix), v0.22.3 (CLI typo guard). Remaining children (TP-6fo, TP-q33, TP-7i3) tracked independently — see discussion notes.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"TP-m43","title":"Phase 7 — Integration tests, benchmarks, release prep v0.20.0","description":"Final integration tests (including PreToolUse-hook composition with a mock second plugin), benchmark suite with p50/p95/p99 targets, README updates, CHANGELOG for v0.20.0, version bump + npm publish readiness. Uses TP-816 §12 + §17.","design":"# Phase 7 — Integration, benchmarks, release prep\n\n## Goal\nAll four components ship together cleanly and performance stays within targets.\n\n## Files\n- Create: tests/e2e/hook-composition.test.ts (installs a mock second PreToolUse hook alongside ours; asserts deny wins)\n- Create: tests/e2e/tp-run-vs-general.test.ts (golden-task comparison, token ratio)\n- Create: scripts/bench-hook.mjs (latency benchmark p50/p95/p99 with cold + warm)\n- Modify: README.md (Quick Start, install-agents section, new modes, tp-* family, marketplace-path placeholder)\n- Modify: CHANGELOG.md (v0.20.0 entry)\n- Modify: package.json (version bump to 0.20.0)\n\n## Acceptance criteria\n- [ ] hook-composition test: mock plugin returns permissionDecision:'allow' + its own additionalContext; our deny-enhanced hook fires; final tool result is denied; both additionalContext strings accumulated into the denial message (if Claude Code behaves as documented) OR at minimum our deny wins (§12 fallback)\n- [ ] Golden comparison test: same onboarding task run under tp-run vs general-purpose fixture harness → tp-run tokens ≤ 40% of general-purpose tokens (regression if slips)\n- [ ] Benchmark thresholds: hook p50 \u003c 30 ms warm, p95 \u003c 100 ms warm, p99 \u003c 250 ms cold. CI fails on breach.\n- [ ] README updated: Quick Start mentions `npx token-pilot install-agents` alongside init; new modes section documents off/advisory/deny-enhanced; tp-* family table; marketplace section marked 'coming in v0.21'\n- [ ] CHANGELOG v0.20.0 entry covers: new modes + auto-migration, SessionStart reminder, bless-agents CLI, tp-* Tier 1 family, install-agents CLI, telemetry upgrades\n- [ ] package.json version = 0.20.0\n- [ ] `npm pack --dry-run` shows dist/agents/*.md bundled, no excluded dist/templates\n- [ ] Full suite green: `npm run lint \u0026\u0026 npm test \u0026\u0026 npm run bench:hook`\n\n## Subtasks\n\n- [ ] **7.1** Write hook-composition E2E test using a small shell harness that simulates Claude Code's hook chain.\n- [ ] **7.2** Write tp-run vs general-purpose golden-task harness (can simulate by running both agents' system prompts against the same scripted task via the agent-builder output + direct LLM call OR via a cheaper stub that counts tool calls — choose pragmatic approach).\n- [ ] **7.3** Write bench-hook.mjs with warm-up + N iterations + percentile reporting.\n- [ ] **7.4** Add `npm run bench:hook` and CI gate.\n- [ ] **7.5** README rewrite — sections Quick Start, Install Agents, Modes, Subagent Family, Marketplace (coming). Preserve existing MCP tool reference.\n- [ ] **7.6** CHANGELOG v0.20.0 entry.\n- [ ] **7.7** Version bump + npm pack dry-run + inspection.\n- [ ] **7.8** Full regression: `npm run lint \u0026\u0026 npm test \u0026\u0026 npm run bench:hook \u0026\u0026 npm pack --dry-run`.\n- [ ] **7.9** Tag git + prepare release notes. Do NOT npm publish — human review before publish. TP-0v4 plan root closed on explicit user sign-off.\n\n## Done-check\nAll above + manual review pass → mark this phase closed. On user confirmation, close TP-0v4 plan root and TP-816 design issue. Start v0.21 work from TP-816 --notes checklist.","notes":"Deferred items for v0.20.x (require live LLM dispatch or Claude Code runner; Phase 7 closed deterministic surface only):\n\n1) Phase 4 carry-over — behavioural assertion 'agent uses MCP tool before any raw Read; response size \u003c= budget + 10%; verdict-first output; no narration phrases'. Deterministic coverage already in Phase 4 (composeAll snapshot, ≤2000 rough-token prompt cap, fixture-agent compat). Needed: live dispatch harness gated on ANTHROPIC_API_KEY or Claude Code test runner; per-agent behavioural test using tests/fixtures/golden-repo/; tool_use JSON assertions; response-size regression gate (\u003e20%% fail per TP-816 §5.4.8).\n\n2) Phase 7 subtask 7.2 — tp-run vs general-purpose golden-task token-ratio assertion (tp-run \u003c= 40%% of general-purpose on same task). Same blocker — measurement requires live LLM; stub would be circular logic. Defer to v0.20.x live-harness work item.\n\n3) Phase 7 'bench:hook CI gate' — currently opt-in via --check=true; default is report-only because per-call Node spawn sits at ~200ms p50. The TP-816 §11 thresholds (warm p50\u003c30ms, p95\u003c100ms, cold p99\u003c250ms) assume the persistent hook-daemon work also listed as future optimisation. When the daemon lands, bench:hook --check=true becomes a real CI gate.\n\nPhase 7 core accepted: hook-composition E2E test (7 assertions on precedence + additionalContext accumulation), bench-hook.mjs script, npm run bench:hook wired, README Quick Start + Hook Modes + Subagent Family + Marketplace-planned sections, CHANGELOG v0.20.0 entry, version bump to 0.20.0, npm pack --dry-run verified (6 tp-*.md bundled, no source leaks). 814 tests green, tsc clean.\n\nPhase 7 NOT closed pending human sign-off (per acceptance 'do NOT npm publish — human review before publish').","status":"closed","priority":1,"issue_type":"feature","owner":"shahinyanm@gmail.com","created_at":"2026-04-17T18:58:25Z","created_by":"shahinyanm","updated_at":"2026-04-18T06:48:41Z","closed_at":"2026-04-18T06:48:41Z","close_reason":"v0.20.0 released. Branch claude/sweet-wilson-5ab84c pushed; tag v0.20.0 created; GitHub release https://github.com/Digital-Threads/token-pilot/releases/tag/v0.20.0 published. npm publish pending human decision. TP-qzj and TP-yk9 (discussion issues) remain for v0.21 planning. Deferred to v0.20.x: live-LLM behavioural assertions + tp-run vs general-purpose ratio + hook-daemon+CI gate for bench thresholds — tracked in TP-m43 notes before close.","dependencies":[{"issue_id":"TP-m43","depends_on_id":"TP-c2a","type":"blocks","created_at":"2026-04-17T22:59:20Z","created_by":"shahinyanm","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"id":"TP-c2a","title":"Phase 6 — Telemetry, config migration, stats","description":"Finalise hook-events.jsonl structure (with agent_type/agent_id), add size-based rotation and retention, extend stats command with --session and --by-agent views, complete config schema migration from legacy v0.19 shapes. Uses TP-816 §15, §14.1, §7.","design":"# Phase 6 — Telemetry, config migration, stats\n\n## Goal\nProduction-quality telemetry and a config-migration pass so v0.19 users upgrade cleanly.\n\n## Files\n- Modify: src/core/hook-tracker.ts (or wherever hook-denied.jsonl is currently written — centralise to .token-pilot/hook-events.jsonl with new schema)\n- Create: src/core/event-log.ts (rotation + retention)\n- Modify: skills/stats/SKILL.md + src/handlers/ (command for --session and --by-agent views)\n- Modify: src/config/loader.ts (auto-migrate legacy mode:\"deny\" → \"advisory\"; emit one-time stderr migration notice; idempotent so second run is silent)\n- Modify: src/types.ts (finalise config shape)\n- Create: tests/core/event-log.test.ts\n- Create: tests/config/migration.test.ts\n\n## Acceptance criteria\n- [ ] Hook-events JSONL schema: {ts, session_id, agent_type, agent_id, event, file, lines, estTokens, summaryTokens, savedTokens}\n- [ ] Rotation: when current file \u003e 10 MB → rename to hook-events.\u003cunix-ts\u003e.jsonl; new file starts empty\n- [ ] Retention: files older than 30 days deleted on startup; total directory size cap 100 MB enforced (oldest deleted first if exceeded)\n- [ ] `token-pilot stats` (current) shows totals + average savings\n- [ ] `token-pilot stats --session` filters to current session_id only (read from env if MCP context provides it, else most recent)\n- [ ] `token-pilot stats --by-agent` groups by agent_type, sorted by savedTokens desc\n- [ ] Config migration: on startup if config has mode:\"deny\" → rewrite to mode:\"advisory\" + emit stderr notice once per file (track migration in config as 'migratedFrom: deny' to avoid double-notice)\n- [ ] env TOKEN_PILOT_DENY_THRESHOLD overrides config.hooks.denyThreshold; documented\n- [ ] Tests cover: schema round-trip; rotation at threshold; retention removes old files only; migration idempotent\n\n## Subtasks\n\n- [ ] **6.1** Centralise hook-event writing: all hook exit paths that currently write to .token-pilot/hook-denied.jsonl now write to hook-events.jsonl with new schema.\n- [ ] **6.2** Implement event-log.ts: append, rotate-if-needed, retention-on-startup. Tests with fake fs time.\n- [ ] **6.3** Extend stats handler with --session and --by-agent flags.\n- [ ] **6.4** Implement config migration for legacy deny mode. Idempotent. Tests with before/after configs.\n- [ ] **6.5** Document new fields in skills/stats/SKILL.md and README config section.\n- [ ] **6.6** Lint + tests green.\n\n## Done-check\nAll tests green + manual smoke ('token-pilot stats --by-agent' on a session with real events) → `bd close \u003cthis-id\u003e`. Phase 7 unblocks.","status":"closed","priority":1,"issue_type":"feature","owner":"shahinyanm@gmail.com","created_at":"2026-04-17T18:57:49Z","created_by":"shahinyanm","updated_at":"2026-04-18T06:22:11Z","closed_at":"2026-04-18T06:22:11Z","close_reason":"Phase 6 done: event-log.ts (rotation 10MB + retention 30d/100MB, wired into hook dispatch with session_id/agent_type/agent_id from stdin), config migration mode:deny→advisory with migratedFrom marker (idempotent), TOKEN_PILOT_DENY_THRESHOLD env override, applyRetention on MCP startup, stats CLI (default/--session/--by-agent), README + stats SKILL docs. 807 tests green, tsc clean. TP-m43 (Phase 7) unblocked.","dependencies":[{"issue_id":"TP-c2a","depends_on_id":"TP-dsi","type":"blocks","created_at":"2026-04-17T22:59:14Z","created_by":"shahinyanm","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"id":"TP-dsi","title":"Phase 5 — install-agents CLI \u0026 bundled templates","description":"Bundle rendered tp-* .md files into dist/agents/ at build time. Ship install-agents and uninstall-agents CLI commands with user-chosen scope (user or project), idempotent update by content-hash, and one-time stderr reminder at MCP startup if agents absent. Uses TP-816 §5.4.5 + §5.4.6.","design":"# Phase 5 — install-agents CLI\n\n## Goal\nShip Tier 1 agents to user's filesystem via explicit CLI command. Scope always chosen by user (user vs project). No silent writes.\n\n## Files\n- Modify: package.json (add \"files\": dist/agents/**/*.md; prepublishOnly hook renders templates → dist/agents)\n- Modify: tsconfig.json or build script (ensure dist/agents copy step)\n- Create: scripts/build-agents.mjs (renders templates/agents/\u003cname\u003e.md via agent-builder.ts → dist/agents/\u003cname\u003e.md)\n- Create: src/cli/install-agents.ts (interactive + non-interactive install)\n- Create: src/cli/uninstall-agents.ts\n- Modify: src/index.ts (add 'install-agents' and 'uninstall-agents' cases)\n- Modify: src/index.ts startServer or wherever MCP starts — add 'agents absent' stderr reminder unless suppressed\n- Modify: src/config/loader.ts (add agents.scope, agents.reminder fields)\n- Modify: src/types.ts (config shape)\n- Create: tests/cli/install-agents.test.ts\n\n## Acceptance criteria\n- [ ] `npm run build` produces dist/agents/tp-*.md (all 6)\n- [ ] `npx token-pilot install-agents` without flags (TTY): prompts 'user/project?' and writes to chosen location\n- [ ] `--scope=user` writes to ~/.claude/agents/tp-*.md\n- [ ] `--scope=project` writes to ./.claude/agents/tp-*.md\n- [ ] Non-TTY without --scope → error 'must specify --scope in non-interactive mode'\n- [ ] After write: stderr prints 'Installed N agents to \u003cscope\u003e. Start a new Claude Code session to see them.'\n- [ ] Idempotence: each written file has frontmatter token_pilot_version + token_pilot_content_hash. Re-running install: if content_hash matches template → update in place silently; if differs but file marked user-edited (hash missing or stale + body modified) → print 'skipping tp-X: edited by user, use --force to override'\n- [ ] `--force` overrides idempotence\n- [ ] `uninstall-agents [--scope=...]` removes only files whose frontmatter shows token_pilot_content_hash present (never touches user files). Scope required (no global default).\n- [ ] agents.scope config field is persisted after first install (so re-runs and upgrades use the same scope without asking)\n- [ ] At MCP server startup: if no tp-* found in any scope AND agents.reminder != false AND TOKEN_PILOT_NO_AGENT_REMINDER not set → emit ONE stderr line: '[token-pilot] tp-* agents not installed. Run `npx token-pilot install-agents` to enable guaranteed-savings subagents (scope: user or project — your choice).'\n- [ ] Reminder is NEVER repeated within same process lifetime\n- [ ] Reminder does not fire if MCP started inside a subagent (detectable via absence of interactive tty? acceptable proxy: skip if env var TOKEN_PILOT_SUBAGENT=1)\n\n## Subtasks\n\n- [ ] **5.1** Write scripts/build-agents.mjs: iterate templates/agents/*.md; for each, run agent-builder to get final content; write to dist/agents/\u003cname\u003e.md. Wire into package.json 'build' or 'prebuild'.\n- [ ] **5.2** Update package.json 'files' array to include dist/agents. Verify `npm pack` contains the agents.\n- [ ] **5.3** Implement install-agents CLI: scope resolution (flag \u003e config \u003e prompt); copy dist/agents/* to target; write frontmatter hash+version.\n- [ ] **5.4** Implement idempotence logic: on re-run, compare frontmatter hash vs current template hash; update unchanged; warn on user-edited; --force override.\n- [ ] **5.5** Implement uninstall-agents CLI with required --scope.\n- [ ] **5.6** Implement MCP startup reminder: on startServer, check target scopes for any tp-*.md with valid frontmatter; if none and not suppressed, emit one stderr line.\n- [ ] **5.7** Add agents.* config fields and env override TOKEN_PILOT_NO_AGENT_REMINDER.\n- [ ] **5.8** Tests per acceptance criteria — use tmp dirs as HOME and CWD in tests to avoid polluting real env.\n- [ ] **5.9** Manual smoke: in a scratch project, run install-agents --scope=project, confirm 6 files in .claude/agents/, verify one has correct frontmatter.\n- [ ] **5.10** Lint + tests green.\n\n## Done-check\nAll acceptance criteria green → `bd close \u003cthis-id\u003e`. Phase 6 unblocks.","status":"closed","priority":1,"issue_type":"feature","owner":"shahinyanm@gmail.com","created_at":"2026-04-17T18:57:20Z","created_by":"shahinyanm","updated_at":"2026-04-18T06:07:28Z","closed_at":"2026-04-18T06:07:28Z","close_reason":"Phase 5 done: build-agents.mjs + dist/agents bundling, install-agents CLI (core + wrapper, idempotence matrix, --force, persisted agents.scope), uninstall-agents (required --scope), MCP startup reminder with single-fire + env/config suppression. 774 tests green, tsc clean. TP-c2a (Phase 6) unblocked.","dependencies":[{"issue_id":"TP-dsi","depends_on_id":"TP-fgo","type":"blocks","created_at":"2026-04-17T22:59:08Z","created_by":"shahinyanm","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"id":"TP-fgo","title":"Phase 4 — tp-* Subagent Templates (6 Tier 1 agents)","description":"Author the 6 Tier 1 tp-* agent markdown templates: tp-run, tp-onboard, tp-pr-reviewer, tp-impact-analyzer, tp-refactor-planner, tp-test-triage. Each uses the shared preamble + output-contract closing block + agent-specific role block. Ships golden fixture repo for CI tests. Uses TP-816 §5.4 in full.","design":"# Phase 4 — tp-* Subagent Templates\n\n## Goal\nProduce 6 Tier 1 agent .md templates stored in templates/agents/ that will later be bundled into dist/agents/ by Phase 5.\n\n## Files\n- Create: templates/agents/tp-run.md\n- Create: templates/agents/tp-onboard.md\n- Create: templates/agents/tp-pr-reviewer.md\n- Create: templates/agents/tp-impact-analyzer.md\n- Create: templates/agents/tp-refactor-planner.md\n- Create: templates/agents/tp-test-triage.md\n- Create: templates/agents/_shared-preamble.md (the ~12-line MCP-first contract)\n- Create: templates/agents/_response-contract.md (closing block per §5.4.9)\n- Create: src/templates/agent-builder.ts (reads shared + specific, produces final md)\n- Create: tests/fixtures/golden-repo/ (small fake project for agent e2e tests)\n- Create: tests/agents/\u003cagent\u003e.test.ts (one per agent, uses golden repo)\n\n## Acceptance criteria\n- [ ] All 6 agents have frontmatter: name, description (task-focused, ≤160 chars, no \u003cexample\u003e blocks), tools (explicit list including mcp__token-pilot__*), optional model/effort\n- [ ] Descriptions use patterns from TP-816 §5.4.3: '[Role]. [Purpose]. Use [PROACTIVELY/when/for] [trigger].' — only tp-run uses PROACTIVELY\n- [ ] Each agent body = shared preamble + response contract + agent-specific role block (role block ≤30 lines)\n- [ ] Each agent's role block declares: 'Response budget: ~N tokens' matching the §5.4.9 table\n- [ ] Shared preamble enforces MUST-use MCP first, raw Read only with offset+limit or honest last resort\n- [ ] Response contract closing block present verbatim from §5.4.9\n- [ ] Golden test per agent: scripted input → agent uses MCP tool before any raw Read; response size ≤ budget + 10%; response contains a one-line verdict; no narration phrases ('I called', 'I ran', 'I looked at')\n- [ ] Prompt-contract CI test: parse each agent's frontmatter, assert tools contains mcp__token-pilot__*, description non-empty, no \u003cexample\u003e blocks in description\n\n## Subtasks\n\n- [ ] **4.1** Write templates/agents/_shared-preamble.md (~12 lines per §5.4.4 of TP-816 design).\n- [ ] **4.2** Write templates/agents/_response-contract.md (6-line closing block from §5.4.9).\n- [ ] **4.3** Write agent-builder.ts: reads shared + response + specific → concatenates → emits final .md. Tests: output is valid md with frontmatter preserved.\n- [ ] **4.4** Write tp-run.md (universal workhorse, PROACTIVELY keyword). Budget: 800.\n- [ ] **4.5** Write tp-onboard.md (project_overview → explore_area → related_files workflow). Budget: 600.\n- [ ] **4.6** Write tp-pr-reviewer.md (smart_diff + outline + find_usages; verdict first, Critical/Important tiers). Budget: 600.\n- [ ] **4.7** Write tp-impact-analyzer.md (find_usages + module_info; blast-radius output). Budget: 400.\n- [ ] **4.8** Write tp-refactor-planner.md (read_for_edit + find_usages + read_diff; plan output with patches to artefact). Budget: 500.\n- [ ] **4.9** Write tp-test-triage.md (test_summary + smart_read + read_range; failing test → root cause → fix). Budget: 500.\n- [ ] **4.10** Set up golden fixture repo under tests/fixtures/golden-repo/: 5-10 files in TS/Python, a fake PR diff, a fake failing test output.\n- [ ] **4.11** Write per-agent golden test: invoke the agent under a mock harness (doesn't need real Claude Code — can simulate via direct prompt call with bundled system prompt), assert MCP tool usage and response shape.\n- [ ] **4.12** Write prompt-contract test: scan all templates/agents/*.md, assert the parsing rules from acceptance criteria.\n- [ ] **4.13** Lint + tests green.\n\n## Verification\n- `npm test -- tests/agents/`\n- Token-count each rendered agent body: ≤ 60 lines incl. frontmatter.\n- Manual sanity-read each agent — does it sound like a single-role specialist, or generic?\n\n## Done-check\nAll agents authored + tests green → `bd close \u003cthis-id\u003e`. Phase 5 unblocks.","status":"closed","priority":1,"issue_type":"feature","assignee":"shahinyanm","owner":"shahinyanm@gmail.com","created_at":"2026-04-17T18:56:41Z","created_by":"shahinyanm","updated_at":"2026-04-18T05:49:16Z","started_at":"2026-04-18T05:34:46Z","closed_at":"2026-04-18T05:49:16Z","close_reason":"Phase 4 done: 6 Tier 1 tp-* agents (tp-run/onboard/pr-reviewer/impact-analyzer/refactor-planner/test-triage), shared preamble, response contract, agent-builder composer, golden fixture, prompt-contract test, deterministic golden tests. 717 tests green, tsc clean. Behavioural acceptance (MCP-first ordering, response-size compliance) carried over to TP-m43 with explicit notes — requires live LLM dispatch.","dependencies":[{"issue_id":"TP-fgo","depends_on_id":"TP-biy","type":"blocks","created_at":"2026-04-17T22:59:03Z","created_by":"shahinyanm","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"id":"TP-biy","title":"Phase 3 — bless-agents CLI (Component 3)","description":"CLI that scans user's installed subagents, classifies them (A/B/C by MCP access), creates project-level overrides for Category-C agents adding mcp__token-pilot__* to their tools list. Includes unbless and doctor drift-check. Uses TP-816 §5.3 + §16.","design":"# Phase 3 — bless-agents CLI\n\n## Goal\nLet users upgrade third-party Category-C subagents to use token-pilot MCP with one command, non-destructively.\n\n## Files\n- Create: src/cli/bless-agents.ts (main command)\n- Create: src/cli/scan-agents.ts (discovers + classifies agent files)\n- Create: src/cli/agent-frontmatter.ts (parse/write YAML frontmatter in agent .md files)\n- Create: src/cli/unbless-agents.ts (reverse operation)\n- Modify: src/index.ts (add 'bless-agents', 'unbless-agents', and doctor subcommands)\n- Modify: src/cli/doctor.ts (or create if not present) — add drift check comparing stored upstream hash vs live upstream file\n- Create: tests/cli/bless-agents.test.ts (fixture tree with mix of A/B/C agents)\n\n## Acceptance criteria\n- [ ] `bd bless-agents` interactive: lists classified agents (A/B skipped, C as candidates); prompts [a]ll / [i]nteractive / [n]o\n- [ ] `--auto` flag non-interactive: blesses all C candidates\n- [ ] `--dry-run` prints what would be written\n- [ ] Each blessed file in ./.claude/agents/\u003cname\u003e.md has frontmatter: inherited name + description; tools list extended with mcp__token-pilot__smart_read, read_symbol, read_for_edit, outline, find_usages, explore_area; token_pilot.{blessed=true, upstream, blessed_at ISO, token_pilot_version, upstream_hash=sha256}\n- [ ] Body of blessed file references upstream via template placeholder {{ upstream_system_prompt }} (resolved at bless time by copying upstream body)\n- [ ] If ./.claude/agents/\u003cname\u003e.md already exists without blessed:true marker → skip (respect prior user override)\n- [ ] `bd unbless-agents [\u003cname\u003e…|--all]` removes blessed files (only those with blessed:true marker)\n- [ ] doctor drift check: recomputes upstream_hash for each blessed file; warns on mismatch with hint 'run token-pilot re-bless \u003cname\u003e'\n- [ ] Stderr notice after write: 'N agents blessed. Start a new Claude Code session to pick them up.'\n- [ ] Built-in Claude Code agents (no filesystem source) are documented as unblessable; CLI prints that note\n\n## Subtasks\n\n- [ ] **3.1** Implement agent-frontmatter helpers: parseFrontmatter(md) → {meta, body}; writeFrontmatter({meta, body}) → md. Parse 'tools:' as comma-separated string OR single '*' OR 'All tools [except X[, Y]]'. Tests.\n- [ ] **3.2** Write scanner: recursively glob .claude/agents/**/*.md + ~/.claude/agents/**/*.md + plugin-contributed agents in ~/.claude/plugins/cache/**/agents/. Parse each. Emit {name, path, scope, tools, description, hash}.\n- [ ] **3.3** Classifier: given parsed agent → 'A' (tools=* or 'All tools'), 'B' (exclusion form and mcp__token-pilot__ not excluded), 'C' (explicit list without mcp__token-pilot__).\n- [ ] **3.4** Implement bless writer: given Category-C agent → build frontmatter (tools extended, token_pilot block) + body from template + upstream body embedded → write to ./.claude/agents/\u003cname\u003e.md atomically (temp + rename).\n- [ ] **3.5** Implement interactive prompt when running without --auto. Use readline, non-TTY fallback = abort with message.\n- [ ] **3.6** Implement unbless: scan blessed files (by marker), remove by name list or --all. Preserve non-blessed overrides.\n- [ ] **3.7** doctor integration: for each blessed file, re-hash upstream content, diff against stored upstream_hash, emit warnings.\n- [ ] **3.8** Wire all 3 CLI commands in src/index.ts main switch.\n- [ ] **3.9** Tests: fixture tree with mix of A/B/C agents; assert only C classified as blessable; bless writes expected files; unbless removes them; doctor detects drift.\n- [ ] **3.10** Lint + tests green.\n\n## Verification\n- `npm test -- tests/cli/`\n- Manual smoke in a scratch dir with fake agents.\n\n## Done-check\nAll acceptance criteria green → `bd close \u003cthis-id\u003e`. Phase 4 unblocks.","notes":"## Progress log (TP-biy Phase 3)\n\n### Completed\n- [x] 3.1 agent-frontmatter (Sonnet#1, commit c915ced) — 20 tests\n- [x] 3.2 scanner (Sonnet#2, commit d07f372)\n- [x] 3.3 classifier (Sonnet#2, commit d07f372) — 15 tests combined\n- [x] 3.4 bless writer (Sonnet#2, commit 7e13262)\n- [x] 3.5 interactive prompt (Sonnet#2, commit 7e13262) — 10 tests combined\n- [x] 3.6 unbless-agents (Opus, commit after cyrillic fix) — 6 tests\n- [x] 3.7 doctor drift detection (Opus) — 8 tests\n- [x] 3.8 wire CLI into main switch (Opus)\n- [x] 3.9 tests covered throughout (55+ total in tests/cli/)\n- [x] 3.10 stderr summary on success (done in 3.4 + 3.6)\n\n### Telemetry (Sonnet subagent cost)\n- Round 1: 74,645 tokens / 30 tool uses (interrupted, finished 3.1)\n- Round 2: 66,682 tokens / 42 tool uses (interrupted, finished 3.2-3.5)\n- Total subagent: 141,327 tokens\n- Opus top-up: ~25k tokens for 3.6, 3.7, 3.8, cyrillic fix\n\n### Notes\n- Both Sonnet rounds were cut off before completion. Parent session\n  (Opus) filled in the remaining subtasks directly. Suggests that\n  Phase 3-class scope (~10 subtasks, fs-heavy CLI + tests) exceeds a\n  single Sonnet budget comfortably — plan to split future phases of\n  similar scope up front.\n- One Sonnet output contained a cyrillic-character identifier bug\n  (unbl\\u0435\\u0441\\u0441Agents). Fixed before commit.\n\nTests: 649/649 passing. Lint + build clean.","status":"closed","priority":1,"issue_type":"feature","owner":"shahinyanm@gmail.com","created_at":"2026-04-17T18:56:03Z","created_by":"shahinyanm","updated_at":"2026-04-17T20:56:33Z","closed_at":"2026-04-17T20:56:33Z","close_reason":"Phase 3 complete: bless-agents / unbless-agents CLI + drift detection shipped. 5 commits (2 Sonnet + 3 Opus cleanup). 649/649 tests green. Unblocks Phase 4 (tp-* templates).","dependencies":[{"issue_id":"TP-biy","depends_on_id":"TP-bn4","type":"blocks","created_at":"2026-04-17T22:58:57Z","created_by":"shahinyanm","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"id":"TP-bn4","title":"Phase 2 — SessionStart Reminder Hook (Component 2)","description":"New PreToolUse-independent hook firing at SessionStart/compact/clear, emitting the combined tools-rules + agent-router additionalContext block. Keeps Claude aware of token-pilot after context resets. Uses design from TP-816 §5.2.","design":"# Phase 2 — SessionStart Reminder Hook\n\n## Goal\nRegister a SessionStart hook that emits one additionalContext block containing the MCP tool rules and the auto-listed tp-* agents present in the project.\n\n## Files\n- Modify: .claude-plugin/hooks/hooks.json (add SessionStart registration)\n- Modify: src/hooks/installer.ts (install SessionStart alongside existing PreToolUse; keep idempotent)\n- Create: src/hooks/session-start.ts (handler — scans agent directories, builds message, returns JSON)\n- Modify: src/index.ts (add 'hook-session-start' case to main switch)\n- Modify: src/types.ts + src/config/loader.ts (add sessionStart.{enabled, showStats, maxReminderTokens})\n- Create: tests/hooks/session-start.test.ts\n\n## Acceptance criteria\n- [ ] hook-session-start CLI entrypoint reads stdin (ignores, SessionStart has no input payload) and emits JSON with additionalContext\n- [ ] Message body is the MANDATORY + WHEN DELEGATING template from TP-816 §5.2, with \u003cauto-list\u003e expanded from actual files found in ~/.claude/agents/tp-*.md and ./.claude/agents/tp-*.md\n- [ ] If no tp-* agents found → the WHEN DELEGATING section is still present but lists 'none installed — run npx token-pilot install-agents'\n- [ ] If sessionStart.enabled is false or config mode is 'off' → hook exits 0 with empty stdout (no reminder)\n- [ ] Total reminder size ≤ sessionStart.maxReminderTokens (default 250 — current verified at 219)\n- [ ] installer.ts registers the new hook idempotently (won't duplicate on re-install)\n- [ ] Test: no agents installed → message says 'run install-agents'\n- [ ] Test: 3 tp-* agents present → message lists those 3 with their descriptions\n- [ ] Test: sessionStart.enabled=false → no output\n\n## Subtasks\n\n- [ ] **2.1** Add sessionStart config shape to types and loader. Defaults: enabled=true, showStats=false (for v0.20; v0.21 flips per TP-816 §21), maxReminderTokens=250.\n- [ ] **2.2** Write agent-scanner helper: scan ~/.claude/agents/ and ./.claude/agents/ for files matching tp-*.md; parse frontmatter; return {name, description} list.\n- [ ] **2.3** Write message-builder: formats the MANDATORY block (static) + WHEN DELEGATING block (from scanner output). Enforce maxReminderTokens cap — if exceeded, trim the delegating list with a '…and N more' suffix.\n- [ ] **2.4** Implement src/hooks/session-start.ts handler combining above; output JSON with hookSpecificOutput.additionalContext.\n- [ ] **2.5** Wire 'hook-session-start' case in src/index.ts main(). Use existing cfg loader pattern.\n- [ ] **2.6** Update installer.ts: add SessionStart registration next to PreToolUse. Idempotent check before append. Update hooks.json template in .claude-plugin.\n- [ ] **2.7** Tests per acceptance criteria above.\n- [ ] **2.8** Lint + tests green: `npm run lint \u0026\u0026 npm test`.\n- [ ] **2.9** Manual verification: start a fresh Claude Code session in a scratch project with token-pilot installed. Check that the reminder appears (visible in model's additionalContext) — or at least that running `node dist/index.js hook-session-start \u003c /dev/null` prints the expected JSON.\n\n## Verification\n- `npm test -- tests/hooks/session-start.test.ts`\n- Token count on the rendered message: ≤ 250.\n\n## Done-check\nAll checkboxes ticked, tests green → `bd close \u003cthis-id\u003e`. Phase 3 unblocks.","notes":"## Progress log (TP-bn4 Phase 2)\n\n### Sonnet subagent work (4 commits)\n- a718769 — sessionStart config section\n- e4f73be — session-start.ts impl + 17 tests\n- 12039bb — wire hook-session-start case\n- 83f17ba — installer + hooks.json SessionStart registration\n\n### Independent harsh-code-reviewer found 2 show-stoppers\n- Show-stopper #1: 'none installed' false message when agents trimmed\n- Show-stopper #2: hooks.mode='off' silently suppressed sessionStart\n\n### Fixes by Opus (main session) after review\n- Fixed both show-stoppers + added 2 regression tests\n- Replaced dynamic import of node:os with static import\n- Commit: (see git log latest on claude/sweet-wilson-5ab84c)\n\n### Deferred (not blocking v0.20)\n- Symlink guard in scanDir (serious #3)\n- isTokenPilotHook substring vulnerability (serious #4 — pre-existing from v0.19)\n- Config parse error noise (serious #6)\n- estimateTokens consolidation (nit #10)\n\nToken accounting (Sonnet subagent + review): 138,524 tokens total.\n\nTests: 592/592 passing. Lint clean. Build clean.","status":"closed","priority":1,"issue_type":"feature","owner":"shahinyanm@gmail.com","created_at":"2026-04-17T18:55:25Z","created_by":"shahinyanm","updated_at":"2026-04-17T20:26:11Z","closed_at":"2026-04-17T20:26:11Z","close_reason":"Phase 2 complete: SessionStart reminder hook shipped via Sonnet subagent (4 commits) + 2 show-stopper fixes from harsh-review (1 Opus commit). Net: 5 commits, 19 session-start tests (17 + 2 regression), 592/592 suite green. Unblocks Phase 3 (bless-agents).","dependencies":[{"issue_id":"TP-bn4","depends_on_id":"TP-d4h","type":"blocks","created_at":"2026-04-17T22:58:51Z","created_by":"shahinyanm","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"id":"TP-d4h","title":"Phase 1 — Enhanced Deny Hook (Component 1)","description":"Upgrade handleHookRead from advisory-style deny to deny-enhanced: block large code Reads and include a full structural summary in permissionDecisionReason. Add the summary pipeline (ast-index subprocess → regex fallback → head+tail → pass-through) and wire it in. Part of TP-0v4 plan, uses design from TP-816 §5.1 + §8.","design":"# Phase 1 — Enhanced Deny Hook\n\n## Goal\nReplace current advisory-style deny in handleHookRead with a structural summary delivered inside permissionDecisionReason.\n\n## Files\n- Modify: src/index.ts (handleHookRead, lines ~165-237)\n- Create: src/hooks/summary-pipeline.ts (orchestrates ast-index → regex → head-tail fallback chain)\n- Create: src/hooks/summary-regex.ts (in-process regex parser)\n- Create: src/hooks/summary-head-tail.ts (trivial last-resort)\n- Create: src/hooks/summary-ast-index.ts (invokes bundled ast-index binary as subprocess)\n- Create: tests/hooks/summary-pipeline.test.ts\n- Create: tests/hooks/handleHookRead.test.ts\n- Modify: src/types.ts (add 'deny-enhanced' to mode union)\n- Modify: src/config/loader.ts (accept deny-enhanced mode; keep advisory; drop deprecated deny with auto-migration to advisory)\n\n## Acceptance criteria\n- [ ] Config mode union is {'off','advisory','deny-enhanced'} — old 'deny' → auto-migrated to 'advisory' with stderr notice\n- [ ] handleHookRead in deny-enhanced mode returns hookSpecificOutput with permissionDecision:deny and permissionDecisionReason containing: header line + Imports section + Exports/Public symbols section + Declarations section + footer hint (bounded Read / read_for_edit / env bypass)\n- [ ] Summary pipeline: tries ast-index subprocess first; falls to regex; falls to head+tail; falls to pass-through (allow) on any catastrophic error\n- [ ] Summary total output respects summaryMaxTokens cap (default 1200)\n- [ ] Non-code extensions pass through (as now)\n- [ ] Bounded Reads (offset or limit present) pass through (as now)\n- [ ] All paths tested: large code file → deny-enhanced; small code file → pass; config file → pass; bounded Read → pass; non-existent path → pass; ast-index missing → regex fallback; regex fails → head-tail; head-tail fails → pass-through\n- [ ] Path-safety check (realpath + startsWith(projectRoot)) on the file argument before reading\n\n## Subtasks (linear order, TDD for each)\n\n- [ ] **1.1** Add 'deny-enhanced' to mode type union in src/types.ts. Drop old 'deny' literal.\n- [ ] **1.2** Update config loader: auto-migrate `mode: \"deny\"` → `\"advisory\"` with a stderr notice; reject unknown modes with clear error. Tests for both migration paths.\n- [ ] **1.3** Write regex summary parser (summary-regex.ts): extracts imports, exports, class/function/interface/type declarations per language. Tests: TS, JS, Python, Go fixtures. Token-count the output.\n- [ ] **1.4** Write head+tail fallback (summary-head-tail.ts): first 40 + last 20 lines with header 'parse failed'. Test with empty file, single-line file, binary-with-code-ext.\n- [ ] **1.5** Write ast-index subprocess bridge (summary-ast-index.ts): findBinary → spawn `ast-index outline \u003cpath\u003e` → parse output → map to unified Summary interface. Test with ast-index present and missing.\n- [ ] **1.6** Write pipeline orchestrator (summary-pipeline.ts): try ast-index → regex → head-tail → pass-through signal. Returns {ok: Summary} | {passThrough: true, reason: string}. Tests for each fallback triggering.\n- [ ] **1.7** Modify handleHookRead in src/index.ts: in deny-enhanced mode, run summary-pipeline; if result is passThrough → exit 0 no output; otherwise format permissionDecisionReason and return the hookSpecificOutput JSON. Preserve existing behaviours for advisory and off modes.\n- [ ] **1.8** Add path-safety check: realpathSync(filePath) must startsWith(projectRoot + path.sep). If not → pass-through + stderr log. Test with ../../etc/passwd style inputs.\n- [ ] **1.9** Integration test: scripted stdin + assert stdout JSON structure for all 5 scenarios (large code, small code, config, bounded, non-existent).\n- [ ] **1.10** Verify lint + all tests green: `npm run lint \u0026\u0026 npm test`.\n- [ ] **1.11** Commit (single commit per subtask is fine; group if needed). Suggested final commit: 'feat(hook): deny-enhanced mode with summary pipeline'.\n\n## Verification commands\n- Lint: `npm run lint`\n- Unit tests: `npm test -- tests/hooks/`\n- Manual smoke: `echo '{\"tool_input\":{\"file_path\":\"src/index.ts\"}}' | node dist/index.js hook-read` → expect JSON with permissionDecision:deny and a structural summary visible in permissionDecisionReason.\n\n## Done-check\nWhen all checkboxes above are ticked AND `npm test` is green AND `git status` is clean: `bd close \u003cthis-id\u003e`. Phase 2 will auto-unblock.","notes":"## Progress log (TP-d4h Phase 1)\n\n### Completed\n- [x] 1.1 HookMode type\n- [x] 1.2 Loader migration + validation\n- [x] 1.3 Regex parser (5 langs)\n- [x] 1.4 Head+tail fallback\n- [x] 1.5 ast-index subprocess\n- [x] 1.6 Pipeline orchestrator\n- [x] 1.7 handleHookRead refactor + formatter\n- [x] 1.8 Path-safety\n- [x] 1.9 CLI integration tests (7 scenarios via child-process spawn)\n\n### Remaining\n- [ ] 1.10 Final verify (lint + full suite already green at each step; this is last-pass sanity)\n- [ ] 1.11 Phase close + unblock Phase 2\n\nTests: 573/573 passing. Build: clean. Ready to close.","status":"closed","priority":1,"issue_type":"feature","assignee":"shahinyanm","owner":"shahinyanm@gmail.com","created_at":"2026-04-17T18:54:48Z","created_by":"shahinyanm","updated_at":"2026-04-17T20:09:56Z","started_at":"2026-04-17T19:07:16Z","closed_at":"2026-04-17T20:09:56Z","close_reason":"Phase 1 complete: 9 subtasks done, 573/573 tests passing, lint + build clean. Enhanced deny hook shipped with full summary pipeline (ast-index → regex → head-tail → pass-through), path-safety, and 7 end-to-end CLI scenarios. Unblocks Phase 2 (SessionStart).","dependency_count":0,"dependent_count":1,"comment_count":0}
{"id":"TP-0v4","title":"IMPL PLAN — token-pilot v0.20 enforcement layer (root)","description":"Implementation plan root for the four-component enforcement layer designed in TP-816. Tracks 7 sequenced phases. Any session can run `bd ready` to find the next available phase; within a phase, the --design field contains checkboxed subtasks to complete in order. Close a phase issue when its acceptance criteria are met to auto-unblock the next phase.","design":"# Implementation Plan — token-pilot v0.20\n\n**Design reference:** TP-816 (full spec in --design of that issue; operational checklist in --notes).\n\n## Resume procedure (for any new session)\n\n1. `bd prime` to load beads context.\n2. `bd ready` → see the next unblocked phase.\n3. `bd show \u003cphase-id\u003e` → read the --design to see checkboxed subtasks + acceptance criteria.\n4. Work subtasks in order. Use `bd update \u003cphase-id\u003e --notes \"progress log…\"` to record status mid-phase for later resume.\n5. When the phase's acceptance criteria are met, `bd close \u003cphase-id\u003e`. The next phase auto-unblocks.\n\n## Phase sequence\n\nPhases run sequentially (each blocks the next). Within a phase subtasks are linear.\n\n1. **Phase 1** — Enhanced Deny Hook (Component 1). New deny-enhanced mode, summary pipeline (ast-index subprocess + regex fallback + head-tail), wired into handleHookRead.\n2. **Phase 2** — SessionStart Reminder (Component 2). New hook handler emitting the combined rules+agent-router message.\n3. **Phase 3** — bless-agents CLI (Component 3). Scanner + classifier + writer + unbless + doctor drift check.\n4. **Phase 4** — tp-* Subagent Templates (Component 4 content). Shared preamble, output-contract closing block, 6 Tier 1 agent .md files, golden fixture repo.\n5. **Phase 5** — install-agents CLI (Component 4 distribution). Bundle templates in dist/agents, install/uninstall with user-chosen scope, MCP-startup stderr reminder, idempotence via frontmatter hash.\n6. **Phase 6** — Telemetry \u0026 Config migration. hook-events.jsonl rotation, stats --by-agent command, config schema + migration (legacy deny → advisory).\n7. **Phase 7** — Integration, Benchmarks, Release. Composition E2E test (multiple PreToolUse hooks), benchmark suite, README + CHANGELOG, version bump, publish.\n\n## TDD discipline\n\nEvery subtask follows the standard cycle (see superpowers:test-driven-development):\n\n1. Write failing test.\n2. Run test, verify failure message is the one you expect.\n3. Write minimal code to pass.\n4. Run test, verify pass.\n5. Refactor if needed (tests still green).\n6. Commit with meaningful message.\n\n## Scope boundary for v0.20.0\n\n- Tier 1 agents only (6 of them). Tier 2 deferred to v0.20.x.\n- No marketplace submission (deferred to v0.21 per TP-816 --notes).\n- No machine-parseable JSON output mode for agents (deferred to v0.21).\n\n## Non-negotiables\n\n- Hook failure paths always pass-through — never abort the agent (§13 of TP-816).\n- No silent writes to user's home dir (§5.4.5).\n- Every 'tp-*' agent's response respects its declared budget in §5.4.9 (CI-enforced).\n- Path safety: realpath + startsWith(projectRoot) check on every file read by the hook.\n\n## Handoff\n\nOn completion of Phase 7 with all acceptance criteria green: update TP-816 status to 'closed', bump package.json to 0.20.0, npm publish, tag git. v0.21 work begins from the TP-816 --notes checklist.","notes":"## Phase ID map (for resume)\n\n| Phase | Beads ID | Title |\n|-------|----------|-------|\n| 1 | TP-d4h | Enhanced Deny Hook (Component 1) |\n| 2 | TP-bn4 | SessionStart Reminder Hook (Component 2) |\n| 3 | TP-biy | bless-agents CLI (Component 3) |\n| 4 | TP-fgo | tp-* Subagent Templates (6 Tier 1 agents) |\n| 5 | TP-dsi | install-agents CLI \u0026 bundled templates |\n| 6 | TP-c2a | Telemetry, config migration, stats |\n| 7 | TP-m43 | Integration tests, benchmarks, release prep v0.20.0 |\n\nFull design reference: TP-816 (--design for spec, --notes for v0.21 checklist).\n\nDep chain: TP-d4h → TP-bn4 → TP-biy → TP-fgo → TP-dsi → TP-c2a → TP-m43.\n\nResume any time with: `bd prime` then `bd ready`. The single unblocked Phase ID is your next work. Inside each Phase's --design are checkboxed subtasks in order.","status":"closed","priority":1,"issue_type":"feature","owner":"shahinyanm@gmail.com","created_at":"2026-04-17T18:54:01Z","created_by":"shahinyanm","updated_at":"2026-04-18T06:48:42Z","closed_at":"2026-04-18T06:48:42Z","close_reason":"v0.20.0 released. Branch claude/sweet-wilson-5ab84c pushed; tag v0.20.0 created; GitHub release https://github.com/Digital-Threads/token-pilot/releases/tag/v0.20.0 published. npm publish pending human decision. TP-qzj and TP-yk9 (discussion issues) remain for v0.21 planning. Deferred to v0.20.x: live-LLM behavioural assertions + tp-run vs general-purpose ratio + hook-daemon+CI gate for bench thresholds — tracked in TP-m43 notes before close.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"TP-816","title":"Design: token-pilot enforcement layer (v0.20 → v0.21)","description":"Four-component architecture (enhanced deny + SessionStart reminder + bless-agents CLI + tp-* subagent family) to enforce token-pilot usage across main agent and all subagent categories without breaking public users. Replaces rejected transparent-substitution design. Target: v0.20.0 ships Tier 1 agents opt-in, v0.21.0 default.","design":"# Design: token-pilot Enforcement Layer\n\n**Status:** Proposed\n**Date:** 2026-04-17\n**Target versions:** v0.20.0 (opt-in) → v0.21.0 (default)\n**Authors:** token-pilot team\n**Reviewers:** system-architect-reviewer (2026-04-17)\n\n---\n\n## 1. TL;DR\n\ntoken-pilot currently relies on an advisory PreToolUse hook and an MCP server. Observations show that (a) agents ignore advisory hints in long sessions, (b) rules are forgotten after `/clear` or `/compact`, and (c) specialised subagents without MCP access cannot use token-pilot tools at all. A previously considered \"transparent Read substitution\" design was found to have show-stopper flaws (Edit corruption, cache-as-file-path poisoning, dangerous line-number drift).\n\nThis document proposes a four-component enforcement layer that fixes all three observations without path substitution and without breaking any existing user:\n\n1. **Enhanced deny mode** — hook returns a structural summary inside `permissionDecisionReason`. Works for every agent, including those with no MCP access.\n2. **SessionStart reminder hook** — re-injects token-pilot rules and available-agent hints after every new session / `/clear` / `/compact`.\n3. **`token-pilot bless-agents` CLI** — interactive tool that creates project-level overrides for restricted subagents so they can opt into token-pilot MCP.\n4. **Subagent family (`tp-*`)** — a set of token-pilot-native subagents (≈11 total, split Tier 1 in v0.20.0 and Tier 2 in v0.20.x) with tightly scoped system prompts that guarantee MCP-first behaviour. Main agent discovers them via standard Claude Code agent registry; reminder in Component 2 lists the right agent per task.\n\n---\n\n## 2. Problem Statement\n\n### 2.1 Observed Pain\n\n| Pain | Symptom | Impact |\n|------|---------|--------|\n| P1 | Agent sees advisory hint, proceeds with Read anyway. | 40–60 % potential savings lost in long sessions. |\n| P2 | After `/clear` or `/compact`, agent acts as if token-pilot doesn't exist. | Rules in `CLAUDE.md` don't propagate into model's active memory. |\n| P3 | Specialised subagents (`acc-*`, many built-ins) have restrictive `tools:` allowlists without `mcp__token-pilot__*`. | Subagents **physically** cannot call token-pilot tools even if instructed. |\n| P4 | A previous idea (hard-block all `Read` on large code files) would break users whose subagents have no MCP fallback. | Unacceptable for a public npm package. |\n\n### 2.2 Non-goals\n\n- Forcing every agent everywhere to adopt token-pilot tool names.\n- Rewriting Claude Code's agent-allowlist model.\n- Providing token savings for non-code files (configs, docs, binaries).\n\n---\n\n## 3. Current State\n\n- PreToolUse hook on `Read`, `Edit` registered via plugin `.claude-plugin/hooks/hooks.json`.\n- `handleHookRead` (src/index.ts:165) denies Read on code files \u003e 300 lines with an advisory message.\n- MCP server exposes ~20 tools (`smart_read`, `read_symbol`, `read_for_edit`, `outline`, `explore_area`, `find_usages`, etc.).\n- Plugin defines SKILL files (`skills/guide`, `skills/install`, `skills/stats`).\n- No SessionStart hook.\n- No per-agent telemetry.\n- No tooling that interacts with third-party agent definitions.\n\n---\n\n## 4. Rejected Alternative: Transparent Read Substitution\n\n**The idea (rejected).** Hook redirects `Read(code.ts)` to a cached summary via `updatedInput.file_path`, so the agent \"thinks\" it read the original.\n\n**Why it fails (architectural review 2026-04-17):**\n\n1. **Edit corruption.** Claude records the cache path as \"the file\"; subsequent `Edit(old_string, new_string)` uses strings from the summary, not the original — edits fail or target wrong regions.\n2. **Mental-model poisoning.** Grep, Write, Bash can all reference the cache path; `Write(cache_path, ...)` silently corrupts the cache.\n3. **Line-number drift.** `L47` in summary ≠ line 47 in the original; `read_range` and shell commands break.\n\nFull rebuttal would require hooking Edit, Write, Grep, Bash, Read with a shared redirection table — unviable for a public package.\n\n**Carried forward:** honest `file_path`, `updatedInput` reserved for sanitisation, explicit signal to the agent when it received a summary. See `docs/superpowers/reviews/...` archive for the full review.\n\n---\n\n## 5. Solution: Four-Component Enforcement Layer\n\n```\n┌─────────────────────────────────────────────────────────────────┐\n│                     Claude Code harness                         │\n│                                                                 │\n│  ┌────────────┐    ┌────────────────┐    ┌─────────────────┐    │\n│  │ Main agent │    │ Subagent (A/B) │    │ Subagent (C)    │    │\n│  │ tools: *   │    │ tools: All     │    │ tools: restricted│    │\n│  └─────┬──────┘    └────────┬───────┘    └────────┬────────┘    │\n│        │                    │                     │             │\n│        │ Read(big.ts)       │ Read(big.ts)        │ Read(big.ts)│\n│        ▼                    ▼                     ▼             │\n│  ┌──────────────────────────────────────────────────────────┐   │\n│  │        PreToolUse hook (always fires, even for C)        │   │\n│  │                                                          │   │\n│  │  Component 1: Enhanced Deny                              │   │\n│  │    → permissionDecision: \"deny\"                          │   │\n│  │    → permissionDecisionReason:                           │   │\n│  │         \"File has N lines. Summary:\\n\u003cstructured\u003e\"       │   │\n│  └──────────────────────────────────────────────────────────┘   │\n│                                                                 │\n│  ┌──────────────────────────────────────────────────────────┐   │\n│  │  SessionStart hook (Component 2)                         │   │\n│  │    → additionalContext: token-pilot rules reminder       │   │\n│  │    → fires on every session / /clear / /compact          │   │\n│  └──────────────────────────────────────────────────────────┘   │\n└─────────────────────────────────────────────────────────────────┘\n\n┌─────────────────────────────────────────────────────────────────┐\n│  Component 3: token-pilot bless-agents (CLI, user-initiated)    │\n│    → scans .claude/agents/ and plugin agents                    │\n│    → offers to create project-level overrides for Category-C    │\n│      agents with mcp__token-pilot__* added to their tools       │\n└─────────────────────────────────────────────────────────────────┘\n```\n\n### 5.1 Component 1 — Enhanced Deny Mode\n\n**What changes.** The existing `handleHookRead` keeps `permissionDecision: \"deny\"` on qualifying Reads but replaces the current advisory message with a **rich structural summary**.\n\n**Flow.**\n\n1. Agent calls `Read(\"src/big.ts\")`.\n2. Hook reads the file, classifies it (code ext? line count \u003e threshold? not bounded?).\n3. If it qualifies, hook runs the **Summary Generation Pipeline** (§8) and builds a message like:\n   ```\n   File \"src/big.ts\" has 2000 lines (~5000 tokens). A structural summary\n   follows; the full Read was denied to save context. For specific lines\n   use Read with offset/limit. For edit context use\n   mcp__token-pilot__read_for_edit(path, symbol). For the full file on\n   purpose use Read with {\"bypass\": true}.\n\n   === Imports ===\n   L2  import ...\n   L3  import ...\n\n   === Exports / Public symbols ===\n   L25 export function getVersion(): string\n   L35 export async function main(cliArgs)\n   L75 export async function startServer(cliArgs)\n   ...\n   ```\n4. Hook returns this as `hookSpecificOutput.permissionDecisionReason`.\n5. Agent sees the Read denied **and already has the structural content** in context. It proceeds:\n   - If it needs specific lines → `Read(path, offset, limit)` (hook passes through).\n   - If it needs edit context → `read_for_edit` via MCP (Categories A, B) or bounded Read (Category C).\n   - If it needs the full file → `Read(path, {bypass: true})` *(see 5.1.3)*.\n\n**Why this is safe.**\n\n- `file_path` is never rewritten. Follow-up tool calls refer to the real file.\n- The agent explicitly knows it received a summary, not content. No line-number confusion because the agent hasn't \"read\" anything it would reference back.\n- No cache file sits in the agent's tool history. No `Write(cache_path, ...)` corruption risk.\n- No Edit hook gymnastics. Edit works normally against the real file.\n- Works for every agent **including Category C** (no MCP required) — the summary arrives as part of the deny message, which every tool caller can read.\n\n**5.1.1 Differences from current deny.**\n\n| Dimension | Current `deny` | Enhanced `deny` |\n|-----------|----------------|-----------------|\n| Reason payload | Advisory: \"use smart_read\" | Structural summary of the file |\n| Agent gets actionable info | No (must retry with different tool) | Yes (summary is already in context) |\n| Works for Category-C agents | No (no MCP fallback) | Yes |\n| Triggers additional tool call | Often | Only when specific lines/edits needed |\n\n**5.1.2 Summary composition contract.**\n\nEach summary contains:\n- Header: original path, line count, estimated token count, agent label.\n- **Imports** block — one line per import (collapsed).\n- **Exports / Public API** block — signature per line, with L-number referring to the original.\n- **Key declarations** block — class/function/type declarations.\n- **Footer**: how to get specifics (bounded Read, `read_for_edit`, `{bypass: true}`).\n\nLine numbers in the summary are **explicitly labelled as original-file line numbers** and the agent is told to use them with `Read(path, offset=L, limit=N)`. This avoids the line-drift class of errors from the rejected substitution design (the agent never looked at a file it thinks is the original).\n\n**5.1.3 Bypass mechanism.**\n\nThree bypass paths, in order of preference for an agent to choose:\n\n1. **Bounded Read** — `Read(path, offset, limit)` is always passed through. Primary way to get specific content.\n2. **New MCP tool `read_full_file_expensive(path)`** — for agents that have MCP access and need the whole file. Named to discourage casual use; telemetry tracks calls.\n3. **Env var `TOKEN_PILOT_BYPASS=1`** — process-wide escape for CI / debug sessions.\n\nA fourth path — `Read(path, {bypass: true})` via a non-standard key in `tool_input` — was considered but dropped from the initial v0.20 scope until we empirically verify (a) that Claude Code's Read tool forwards unknown keys to the hook across different subagent types, and (b) that subagents with strict input schemas don't reject the call client-side. Test plan in §17.6. If the test passes in v0.20.x, we add it as a fourth path in v0.21 without breaking changes.\n\n**5.1.4 Why no separate reminder for subagents.**\n\nSubagents run with their own prompt context; the SessionStart hook targets the top-level session (see §18 Q7). For Category-A/B subagents with MCP access, the enhanced-deny message delivered on their first qualifying Read carries the full instruction set (summary + bypass options + MCP tool names). For Category-C subagents without MCP access, the same deny message still arrives and is equally actionable (bounded Read, env var). In both cases, the hook's own output is the reminder — no additional injection is needed. If telemetry in v0.20.x shows that subagents persistently fail to use bounded Read even after seeing the deny message, we will revisit with a per-first-call `additionalContext` reminder.\n\n### 5.2 Component 2 — SessionStart Reminder Hook\n\n**What changes.** New hook registered in `.claude-plugin/hooks/hooks.json`:\n\n```json\n{\n  \"hooks\": {\n    \"SessionStart\": [\n      {\n        \"hooks\": [\n          {\"type\": \"command\", \"command\": \"${NODE} ${SCRIPT} hook-session-start\"}\n        ]\n      }\n    ]\n  }\n}\n```\n\n**Behaviour.** On every session start, `/clear`, or `/compact`, the hook emits a single `additionalContext` block combining the mandatory-tool rules **and** agent-router guidance:\n\n```\n[token-pilot active]\n\nMANDATORY — for code files, use these before raw Read:\n  - mcp__token-pilot__smart_read(path)          — structural overview\n  - mcp__token-pilot__read_symbol(path, sym)    — one function / class\n  - mcp__token-pilot__read_for_edit(path, sym)  — exact text for editing\n  - mcp__token-pilot__outline(path)             — symbol list\nRaw Read is allowed only with offset/limit, or with TOKEN_PILOT_BYPASS=1.\n\nWHEN DELEGATING — use the right token-pilot-native subagent:\n  - tp-run              — general workhorse, MCP-first drop-in for general-purpose\n  - tp-onboard          — exploring an unfamiliar repo\n  - tp-pr-reviewer      — reviewing a diff or changeset\n  - tp-impact-analyzer  — tracing what will break from a change\n  - tp-refactor-planner — planning a refactor with precise context\n  - tp-test-triage      — investigating test failures\n  (Additional tp-* agents installed in this project: \u003cauto-list\u003e)\n\nBudget summary: last session saved ~N tokens across M reads.\n```\n\nThis reminder is **short by design** (~250 tokens including the agent list) — compact enough to not bloat the prompt, specific enough that the model remembers both the tool contract and the agent registry.\n\nThe `\u003cauto-list\u003e` placeholder is expanded at hook run-time by scanning `.claude-plugin/agents/` and `.claude/agents/` for `tp-*` names. Tier 2 agents installed later auto-appear without spec edits.\n\nThe reminder is only emitted if (a) `.token-pilot/` exists (token-pilot is active in this project) and (b) the mode is not `off`. Safe by default.\n\n### 5.3 Component 3 — `token-pilot bless-agents` CLI\n\n**What it does.** Helps users give Category-C agents full MCP access to token-pilot.\n\n**Flow.**\n\n```\n$ token-pilot bless-agents\nScanning installed agents...\n  ✓ general-purpose            tools: *               → already has MCP access\n  ✓ code-analyzer              tools: All tools       → already has MCP access\n  ✗ acc-ddd-auditor            tools: Read, Bash,...  → MISSING token-pilot MCP\n  ✗ acc-behavioral-auditor     tools: Read, Grep,...  → MISSING token-pilot MCP\n  ✗ Explore                    tools: All tools except Write, Edit\n                                       → already has MCP access (exclusion)\n  ...\n\nFound 14 agents that would benefit from token-pilot MCP access.\nCreate project-level overrides with MCP tools added?\n\n  [a] Yes, all\n  [i] Interactive (ask per agent)\n  [n] No, cancel\n\n\u003e a\n\nCreating overrides in .claude/agents/...\n  ✓ .claude/agents/acc-ddd-auditor.md (added mcp__token-pilot__*)\n  ✓ .claude/agents/acc-behavioral-auditor.md (added mcp__token-pilot__*)\n  ...\n\nDone. 14 agents now have access to token-pilot MCP.\nOriginal definitions remain intact — run `token-pilot unbless-agents` to remove overrides.\n```\n\n**How it works.**\n\n1. Scan discovery locations in priority order: project `.claude/agents/`, user `~/.claude/agents/`, plugin-contributed agents.\n2. For each agent, parse frontmatter `tools:` field.\n3. Classify:\n   - **A** — `tools: *` or `All tools` → already has MCP access → skip.\n   - **B** — `tools: All tools except ...` and `mcp__token-pilot__*` not excluded → has access → skip.\n   - **C** — explicit list without `mcp__token-pilot__*` → candidate for bless.\n4. For each Category-C agent:\n   - If project override already exists → skip (respect user choice).\n   - Otherwise, write a new project-level `.md` that *references* the upstream agent and adds MCP tools.\n\n**Override file format.**\n\n```markdown\n---\nname: acc-ddd-auditor\ndescription: (inherited from upstream)\ntools: Read, Bash, Grep, Glob, Task, TaskCreate, TaskUpdate, mcp__token-pilot__smart_read, mcp__token-pilot__read_symbol, mcp__token-pilot__read_for_edit, mcp__token-pilot__outline, mcp__token-pilot__find_usages\ntoken_pilot:\n  blessed: true\n  upstream: plugin\n  blessed_at: \"2026-04-17T20:00:00Z\"\n  token_pilot_version: \"0.20.0\"\n---\n\n# acc-ddd-auditor (token-pilot blessed)\n\nThis agent's tool allowlist was extended with token-pilot MCP tools by\n`token-pilot bless-agents`. The upstream system prompt and behaviour are\npreserved by delegation.\n\n{{ upstream_system_prompt }}\n```\n\nThe `{{ upstream_system_prompt }}` template placeholder is resolved at bless time by copying the upstream file's body. If the upstream changes, users are warned by `token-pilot doctor` (§16.4).\n\n**Safety properties.**\n\n- **Opt-in.** Nothing runs without user invoking `bless-agents`.\n- **Reversible.** `unbless-agents` removes all blessed files.\n- **Non-destructive.** Only creates new project-level overrides; never edits upstream.\n- **Discoverable drift.** `doctor` checks upstream agent hashes; warns if upstream changed after blessing.\n- **Respects user's prior overrides.** If `.claude/agents/\u003cname\u003e.md` already exists without the `blessed` marker, skip it.\n\n### 5.4 Component 4 — Subagent Family (`tp-*`)\n\n**Intent.** Ship token-pilot-native subagents whose tool allowlists and system prompts we fully control. Users that invoke them get a hard guarantee of MCP-first behaviour — no advisory, no blessing, no drift risk. This complements (does not replace) the other three components: the hook is still the universal safety net; the SessionStart reminder makes the family discoverable; `bless-agents` upgrades third-party agents users already rely on.\n\n**Why this is the right shape.** Most token-pilot tools are useless in isolation and valuable in workflows (`project_overview` → `explore_area` → `read_symbol`; or `smart_diff` + `outline` + `find_usages` for review). An agent encapsulates one workflow, picks its own 3–5 tools, and gives the main agent a single named entrypoint for the whole job. This covers all 20 MCP tools through the agent layer, not just the 2–3 the hook typically triggers.\n\n#### 5.4.1 Tier 1 — v0.20.0 (6 agents, MUST ship)\n\n| Agent | User intent (\"I want to…\") | Core tools |\n|-------|---------------------------|------------|\n| `tp-run` | \"do something general, save tokens\" | any (MCP-first) |\n| `tp-onboard` | \"I'm new to this repo, orient me\" | `project_overview`, `explore_area`, `related_files`, `outline` |\n| `tp-pr-reviewer` | \"review this diff / changeset\" | `smart_diff`, `outline`, `find_usages`, `read_symbol` |\n| `tp-impact-analyzer` | \"what will break if I change X?\" | `find_usages`, `module_info`, `related_files` |\n| `tp-refactor-planner` | \"plan a refactor with exact context\" | `read_for_edit`, `find_usages`, `read_diff`, `outline` |\n| `tp-test-triage` | \"understand why tests are failing\" | `test_summary`, `smart_read`, `read_range`, `find_usages` |\n\n#### 5.4.2 Tier 2 — v0.20.x (5 agents, NICE-TO-HAVE, ship as telemetry justifies)\n\n| Agent | User intent | Core tools |\n|-------|-------------|------------|\n| `tp-dead-code-hunter` | \"find dead code\" | `find_unused`, `find_usages` |\n| `tp-api-surveyor` | \"what does this module expose as public API?\" | `module_info`, `outline`, `find_usages` |\n| `tp-history-explorer` | \"what happened in this area recently?\" | `smart_log`, `smart_diff`, `outline` |\n| `tp-doc-navigator` | \"structurally read docs / configs\" | non-code section handlers (md / yaml / json / csv) |\n| `tp-audit-scanner` | \"find TODOs, FIXMEs, smells\" | `code_audit`, `find_usages` |\n| `tp-session-restorer` | \"restore prior session context\" | `session_snapshot` |\n\n#### 5.4.3 Naming convention\n\n- **Task-focused, not tool-focused.** Name encodes the intent the user brings, not the MCP tool used under the hood. `tp-impact-analyzer` \u003e `tp-find-usages-agent`.\n- **Verb or role.** Every name is a recognisable role (`run`, `reviewer`, `planner`, `hunter`, `surveyor`, `navigator`, `triage`) or an -er/-or noun. This is how the main agent recognises \"this subagent can do this kind of work\".\n- **`tp-` prefix is load-bearing.** It groups the family in the agent registry, tells the main agent \"MCP-first guarantee here\", and is the hook of the family brand. Non-negotiable.\n\n#### 5.4.4 System prompt architecture\n\nEvery `tp-*` agent shares a **base preamble** and adds an **agent-specific role block**. This keeps prompts short and consistent; a change to the token-pilot contract touches one file.\n\n**Base preamble (shared, ~12 lines).**\n```\nYou are a token-pilot agent (`tp-\u003cname\u003e`). Your defining contract:\n\nFor every file in a programming language, you MUST use the token-pilot MCP tools\n(`mcp__token-pilot__smart_read`, `read_symbol`, `read_for_edit`, `outline`,\n`find_usages`, `explore_area`, `project_overview`) before considering raw Read.\nRaw Read is allowed only with explicit offset/limit, or when MCP tools have\nalready been tried and do not fit the task — in which case you must say so\nin your reasoning. Never dump a file's full contents unless absolutely\nnecessary.\n\nIf any MCP tool fails, fall back sensibly (another MCP tool → bounded Read →\npass-through) and note the fallback in your output. Never silently abandon\nthe contract.\n\nYour specific role is defined below.\n```\n\n**Agent-specific role block (~10–30 lines per agent).** Describes the workflow this agent encodes, which tools it primarily reaches for, and when it should hand back to the caller. Example for `tp-impact-analyzer`:\n\n```\nRole: impact analysis. When given a symbol, file, or change description,\nyou must:\n  1. Locate the change surface via `read_symbol` or `outline`.\n  2. Enumerate downstream dependents via `find_usages` (direct + transitive\n     to one hop).\n  3. Inspect each dependent with `read_symbol` or bounded Read to judge\n     compatibility.\n  4. Report: list of affected sites, compatibility verdict per site, and a\n     blast-radius summary.\n\nDo not propose fixes. Your job is the honest impact map.\n```\n\n#### 5.4.5 Packaging \u0026 distribution\n\n**Key constraint.** token-pilot is distributed primarily as an npm package, not as a Claude Code plugin via marketplace. Empirically verified (2026-04-17): `.claude-plugin/agents/*.md` is **only** auto-loaded when the plugin has been installed through Claude Code's plugin system (entry in `~/.claude/plugins/installed_plugins.json`). A typical `npm install token-pilot` user does not get this path; their Claude Code scans `~/.claude/agents/` and the project's `.claude/agents/` for agent registry.\n\n**Distribution model for v0.20.**\n\nAgent markdown files are bundled in the npm package at `dist/agents/tp-*.md` (built from source `templates/agents/tp-*.md`). They are NOT placed in `.claude-plugin/agents/` because that would only work for marketplace-installed plugins. Instead, a CLI command copies them into one of the two standard locations Claude Code actually scans.\n\n**The CLI is the canonical path; scope is always the user's explicit choice, mirroring Claude Code's own `/plugin install` UX.**\n\n```bash\n# Interactive (TTY) — asks the user where to install\nnpx token-pilot install-agents\n\n  Where should token-pilot agents be installed?\n    [1] user     → ~/.claude/agents/tp-*.md (available in every project)\n    [2] project  → .claude/agents/tp-*.md (only this project)\n  Choice [1/2]: _\n\n# Non-interactive — script / CI form\nnpx token-pilot install-agents --scope=user\nnpx token-pilot install-agents --scope=project\n\n# Removal\nnpx token-pilot uninstall-agents\nnpx token-pilot uninstall-agents --scope=user\n```\n\n**No silent auto-install.** At MCP server startup, if no `tp-*` agents are detected in either scope, the server emits exactly ONE stderr line:\n\n```\n[token-pilot] tp-* agents not installed. Run `npx token-pilot install-agents` to enable guaranteed-savings subagents (scope is your choice: user or project).\n```\n\nThe reminder can be silenced with `TOKEN_PILOT_NO_AGENT_REMINDER=1` or `agents.reminder: false` in config. It never auto-installs, never prompts inside the MCP server (MCP has no TTY), and never writes files the user has not explicitly requested.\n\n**Idempotence and versioning.**\n\n- Each installed agent file has `token_pilot_version: \"\u003csemver\u003e\"` and `token_pilot_content_hash: \"\u003csha256-of-template\u003e\"` in its frontmatter.\n- Re-running `install-agents` updates agent files whose `content_hash` matches the prior version's template (i.e., the user did not edit them). Hand-edited files are preserved and the CLI emits `\"⚠ tp-\u003cname\u003e.md edited by user, skipping update.\"`.\n- A `--force` flag on `install-agents` overrides and re-installs from templates, with a confirmation prompt.\n\n**Parallel path — Claude Code marketplace submission (Vision for v0.21+).**\n\nIndependently of the CLI path, we submit token-pilot as a proper Claude Code plugin to one of the public marketplaces (`anthropics/claude-plugins-official` or our own). Users then have two installation options:\n\n1. **Via npm** (current, CLI-driven agent install): `npm install token-pilot` + `npx token-pilot install-agents`.\n2. **Via Claude Code**: `/plugin install token-pilot`. The plugin bundle includes `.claude-plugin/agents/tp-*.md`, `.claude-plugin/hooks/hooks.json`, and the MCP manifest. Auto-loads everything natively.\n\nBoth paths converge on the same agent definitions — the marketplace package simply ships the same files in the plugin-native location. No fork, no divergence.\n\nThe marketplace path is intended for v0.21+ — it requires coordination with Claude Code release cycles and is not a v0.20 blocker. v0.20 ships solely through npm.\n\n#### 5.4.6 Adoption mechanism (no new component)\n\nWe explicitly do **not** add a separate \"agent router\" component. The family is discovered via three existing channels:\n\n1. **SessionStart reminder (§5.2).** Lists the installed `tp-*` agents once per session with one-line descriptions.\n2. **Agent frontmatter `description:` field.** Claude Code shows this when the main agent is choosing a delegate. Descriptions are written task-first (see §5.4.3).\n3. **README / CHANGELOG.** The public-facing documentation path.\n\nNo CLAUDE.md auto-mutation. No invisible routing. Main agent chooses based on the normal Claude Code delegation flow; our contribution is making the right choice obvious.\n\n#### 5.4.7 Tier gating\n\n- Tier 1 ships in v0.20.0. Six agents are enough to cover the high-frequency workflows (general work, onboarding, review, impact, refactor, tests).\n- Tier 2 ships agent-by-agent in v0.20.x, gated by telemetry: we ship the next agent when the SessionStart reminder's corresponding task-intent log shows ≥ 5 uses per active user per week without a matching `tp-*` being invoked. This prevents prompt bloat from agents users don't reach for.\n- Maintenance ceiling: 15 agents. Beyond that, we reject additions until we decommission one. Keeps the family navigable in the reminder and the main agent's delegate list.\n\n#### 5.4.8 Testing strategy (agent-specific, complements §17)\n\n- **Per-agent smoke test.** A scripted task (fixture repo + prompt) runs under the agent and asserts: (a) MCP tool called at least once before any raw Read, (b) raw-Read count ≤ threshold, (c) output contains the expected artefact shape.\n- **Golden comparison.** Same task run under `tp-run` vs `general-purpose` — assert `tp-run` uses ≤ 40 % of the tokens used by `general-purpose` on the fixture. Regression if this slips.\n- **Prompt contract test.** Parse each agent's frontmatter + body, verify the shared preamble is present, tools list contains `mcp__token-pilot__*`, description is non-empty and non-generic.\n- **Behaviour drift alert.** If an agent's token usage on the golden task increases \u003e 20 % between versions, CI fails. Protects against accidental prompt edits that weaken the MCP-first discipline.\n- **Output-budget test.** Each agent's response on the golden fixture is measured. Regression if response exceeds the §5.4.9 budget by \u003e 10 %. Protects the caller's context from accidental bloat.\n\n#### 5.4.9 Output contract and context hygiene\n\nA subagent that saves tokens on reads but returns a 3 000-token verbatim dump gives it all back. Output shape is as important as read efficiency.\n\n**Adopt (from feature-dev agents):** numeric confidence gate with hard threshold; \"pick one, commit\" (no multi-option presentations); terminal \"essential files\" pointer list; `file:line` refs over pasted code.\n\n**Forbid explicitly:** mandatory positive preamble; \"comprehensive\" without cap; \"thorough but concise\" (contradictory); inline code when `file:line` suffices; `\u003cexample\u003e` blocks in `description:` frontmatter (they ship on every delegate-listing); tool-call narration (\"I called smart_read, then…\").\n\n**Output contract enforced in every agent's preamble:**\n\n1. Verdict on line 1 — the caller can stop there.\n2. Named bold sections; one bullet = one finding.\n3. Filter to a declared bar (confidence ≥ N, severity ≥ X) — below the bar → not shown.\n4. `file:line` refs only; no pasted source unless the role requires a patch.\n5. No tool narration, no operational log.\n6. Overflow → artefact: findings over budget write to `.token-pilot/\u003cagent\u003e-\u003cts\u003e.md`; visible response stays within budget.\n\n**Per-agent response budgets (tokens, tightenable in v0.20.x):**\n\n| Agent | Budget | Agent | Budget |\n|-------|--------|-------|--------|\n| `tp-run` | 800 | `tp-onboard` | 600 |\n| `tp-pr-reviewer` | 600 | `tp-impact-analyzer` | 400 |\n| `tp-refactor-planner` | 500 | `tp-test-triage` | 500 |\n| `tp-dead-code-hunter` | 400 | `tp-api-surveyor` | 500 |\n| `tp-history-explorer` | 500 | `tp-doc-navigator` | 400 |\n| `tp-audit-scanner` | 500 | `tp-session-restorer` | 300 |\n\nEnforced via §5.4.8 output-budget test.\n\n**Preamble closing block (appended to the shared preamble in §5.4.4):**\n\n```\nRESPONSE CONTRACT:\n- Lead with a one-line verdict.\n- Use bold section headers; one finding per bullet.\n- Reference code as `path:line`; paste source only if your role requires a patch.\n- Do NOT narrate tool calls. Do NOT preamble with \"what was done well\".\n- If findings exceed your budget, write overflow to\n  `.token-pilot/\u003cagent\u003e-\u003ctimestamp\u003e.md` and reference it; keep the\n  visible response within budget.\n```\n\n**Machine-parseable output (v0.21+).** Each `tp-*` may emit a JSON block guarded by `--format=json` for agent chaining. Schema per agent post-marketplace. Not v0.20 scope.\n\n---\n\n## 6. Subagent Categories and Outcomes\n\n| Category | Definition | Example agents | What fixes them |\n|----------|------------|----------------|-----------------|\n| A | `tools: *` / `All tools` | general-purpose, code-analyzer, test-writer, business-logic-auditor | SessionStart reminder + normal MCP usage. No changes needed. |\n| B | `tools: All tools except X` where MCP not excluded | Explore, Plan, beads:task-agent | Same as A. |\n| C | Explicit list, no MCP | acc-\\*, many specialised built-ins | Either (i) **Enhanced deny** gives them a summary in the deny message (no MCP needed), or (ii) user runs `bless-agents` to grant MCP access. |\n\nBoth paths for Category C are available; `bless-agents` is the opt-in upgrade, enhanced deny is the always-on baseline.\n\n---\n\n## 7. Modes \u0026 Configuration\n\n### 7.1 Modes\n\n| Mode | Behaviour | Intended use |\n|------|-----------|--------------|\n| `off` | Hook does nothing. | Debug / CI / opt-out. |\n| `advisory` | Short hint only, like current v0.19. | Power users who want minimal intervention. |\n| `deny-enhanced` | Deny with structural summary in reason. | **New default from v0.21.** |\n\nThe old `deny` mode (v0.19 behaviour — deny with only a short tip, no summary) is removed. It offered no advantage over `deny-enhanced`, which is a strict superset. Users who prefer minimal intervention can switch to `advisory`.\n\n### 7.2 Configuration file\n\n`.token-pilot.json` at the root directory where Claude Code was launched (i.e. its starting CWD). Falls back to `~/.config/token-pilot/config.json` for global defaults. No hierarchical lookup — the config at the launch CWD wins, period. This keeps behaviour predictable in monorepos and worktrees: whatever directory you opened Claude Code in, that's the config it reads.\n\n```json\n{\n  \"mode\": \"deny-enhanced\",\n  \"hooks\": {\n    \"denyThreshold\": 150,\n    \"summaryMaxTokens\": 1200,\n    \"excludePatterns\": [\"**/node_modules/**\", \"**/dist/**\"],\n    \"includeOnlyCodeExt\": true\n  },\n  \"sessionStart\": {\n    \"enabled\": true,\n    \"showStats\": true,\n    \"maxReminderTokens\": 200\n  },\n  \"cache\": {\n    \"enabled\": false,\n    \"comment\": \"Enhanced deny mode does not need file cache; reserved for future use.\"\n  },\n  \"agents\": {\n    \"scope\": null,\n    \"reminder\": true,\n    \"comment\": \"scope is null until user runs `install-agents`; CLI writes user|project here. reminder=false suppresses the stderr nudge at MCP startup.\"\n  }\n}\n```\n\n### 7.3 Env-var overrides\n\n| Variable | Effect |\n|----------|--------|\n| `TOKEN_PILOT_MODE=off` | Disable hook. |\n| `TOKEN_PILOT_BYPASS=1` | Pass through all Reads for this process. |\n| `TOKEN_PILOT_DENY_THRESHOLD=300` | Override line threshold. |\n| `TOKEN_PILOT_DEBUG=1` | Verbose hook logging to stderr. |\n| `TOKEN_PILOT_NO_AGENT_REMINDER=1` | Suppress the \"install-agents\" reminder at MCP startup. |\n\n---\n\n## 8. Summary Generation Pipeline\n\nThe deny-enhanced message embeds a structural summary. Quality matters — poor summaries push the agent to re-read the file anyway, losing savings.\n\n**Pipeline (fallback chain):** the hook is a short-lived Node process. It cannot share state with the long-running MCP server (MCP uses stdio to Claude Code), so parsers must run in-process or delegate to a separate binary.\n\n1. **Primary — ast-index binary subprocess.** If `findBinary` locates the bundled ast-index, hook invokes `ast-index outline \u003cfile\u003e` and parses the structured output. Gives exports, signatures, line refs. Typical ~20–60 ms warm.\n2. **Fallback 1 — in-process regex parser.** Pattern-based scan for language-specific declarations (mirrors the prototype). Pure Node, no subprocess. Typical ~5–20 ms.\n3. **Fallback 2 — truncated head + tail.** Last-resort structural proxy when parser crashes: first 40 lines + last 20 lines with a clear \"parse failed, giving you head/tail\" warning. Trivial cost.\n4. **Fallback 3 — pass-through.** If even fallback 2 fails (disk error, encoding issue), emit `permissionDecision: \"allow\"` to let the original Read proceed. **Never return an error that blocks the agent.**\n\n**Budget.** Summaries are capped at `summaryMaxTokens` (default 1200). If exceeded, the summary is trimmed and a note added: `(trimmed to fit budget; call outline(path) for full structure)`.\n\n**Language support.** Primary parser supports TS/JS/Python/Go/Rust/Java/PHP/Ruby. For unsupported extensions, fallback 1 (regex) handles them coarsely.\n\n---\n\n## 9. Hook Implementation Details\n\n### 9.1 Hook input contract\n\nExpected stdin (from Claude Code):\n\n```json\n{\n  \"session_id\": \"...\",\n  \"transcript_path\": \"...\",\n  \"cwd\": \"...\",\n  \"permission_mode\": \"default\",\n  \"hook_event_name\": \"PreToolUse\",\n  \"tool_name\": \"Read\",\n  \"tool_input\": { \"file_path\": \"...\", \"offset\": null, \"limit\": null },\n  \"agent_id\": \"...\",     // optional, present for subagents\n  \"agent_type\": \"...\"    // optional, present for subagents\n}\n```\n\n### 9.2 Hook output contract (enhanced deny)\n\n```json\n{\n  \"hookSpecificOutput\": {\n    \"hookEventName\": \"PreToolUse\",\n    \"permissionDecision\": \"deny\",\n    \"permissionDecisionReason\": \"\u003csummary text\u003e\"\n  }\n}\n```\n\nFor pass-through: `exit 0` with empty stdout.\nFor bypass (flag or bounded): `exit 0` with empty stdout.\nFor failure: `exit 0` with empty stdout (degrade to pass-through; log to stderr).\n\n### 9.3 Hook process lifecycle\n\n**Latency target.** p50 \u003c 30 ms, p95 \u003c 100 ms, p99 \u003c 250 ms.\n\n**Strategy.**\n\n- Hook script is short-lived Node process; cold start ~80–150 ms (Node init + script parse).\n- Primary parser invokes `ast-index` binary as subprocess (~20–60 ms warm via OS fork/exec).\n- Fallback to in-process regex parser (pure Node, ~5–20 ms) if `ast-index` is unavailable.\n- Fallback to head+tail view (~5 ms) if regex parser errors.\n\n**Future optimisation (out of scope for v0.20).** Long-running hook daemon with persistent ast-index state, reducing warm latency to ~5–10 ms. Deferred until benchmarks show the sub-process strategy is a real UX problem. Design leaves room for this: swap the parser invocation layer without changing the hook contract.\n\n---\n\n## 10. Security\n\n### 10.1 Path safety\n\n- **Problem:** `resolve(cwd, path)` does not prevent `../../../etc/passwd`.\n- **Mitigation:** `realpath` the resolved path; require it to `startsWith(projectRoot + path.sep)`; deny-with-error-pass-through otherwise.\n\n### 10.2 Summary content leakage\n\n- **Concern:** Summary reveals structural info about private code.\n- **Mitigation:** Summary is emitted only back to the same session that requested the Read. It is never written to disk in enhanced-deny mode (cache removed from design). Stderr logging is opt-in via `TOKEN_PILOT_DEBUG`.\n\n### 10.3 Hook input trust\n\n- Hook input comes from Claude Code. Treated as untrusted — validate all fields (no `JSON.parse` of unchecked size, string-length caps on paths, refuse `file_path` containing null bytes).\n\n### 10.4 CI / sandboxed environments\n\n- If the hook cannot resolve `projectRoot` (no git, read-only FS, sandbox), it emits pass-through and logs a single stderr note. Must **never** abort the agent's workflow.\n\n---\n\n## 11. Performance \u0026 Latency\n\n| Operation | Target | Notes |\n|-----------|--------|-------|\n| Hook cold start (Node) | \u003c 150 ms | Unavoidable today; mitigated because hook only fires on large code Reads. |\n| Summary via MCP (warm) | \u003c 30 ms | IPC + ast-index lookup. |\n| Summary via regex | \u003c 30 ms | Pure Node, no IPC. |\n| Summary via head+tail | \u003c 5 ms | Trivial. |\n| End-to-end hook (warm) | \u003c 100 ms p95 | Combines cold start + summary. |\n| End-to-end hook (cold) | \u003c 250 ms p99 | Includes Node cold start in adverse environments. |\n\n**Overhead budget.** For a session with 30 qualifying Reads, worst-case hook overhead is ~7.5 s cold or ~3 s warm. In exchange for ~70 % token reduction, this is a justified trade. We will ship benchmarks in `docs/performance/2026-04-17-hook-benchmark.md` to validate.\n\n**Mitigation levers available later.** Persistent hook daemon, bytecode cache, skipping the MCP IPC for small files.\n\n---\n\n## 12. Hook Chain Composition\n\n**Problem.** Users may have multiple plugins registering `PreToolUse:Read` hooks (e.g. `context-mode` seen in the wild).\n\n**Claude Code behaviour (confirmed from official docs, 2026-04-17 verification).**\n\nClaude Code defines an explicit precedence for resolving conflicting PreToolUse decisions:\n\n\u003e *\"When multiple PreToolUse hooks return conflicting decisions, the system follows a specific precedence order: **deny, defer, ask, and finally allow**. This ensures that restrictive decisions take priority over permissive ones.\"*\n\u003e\n\u003e *\"A PreToolUse hook returning `deny` will cancel the tool call regardless of other hook outcomes.\"*\n\u003e\n\u003e *\"Text from `additionalContext` is accumulated from all hooks and passed to Claude.\"*\n\nThis resolves the ambiguity we previously worried about. Our design benefits directly:\n\n1. **Enhanced deny wins over any competing `allow`.** If another plugin's hook says \"allow\" and ours says \"deny\", our deny is final. Our users get our summary regardless of what else is installed.\n2. **SessionStart reminder is additive, not exclusive.** Our reminder text is accumulated with any other plugin's `additionalContext`, not replaced. We never fight another plugin for the reminder slot.\n3. **Our design never uses `updatedInput`** (see §4 for why), so the one remaining undefined-behaviour area — `updatedInput` chaining across hooks — is not on our critical path.\n\n**Remaining edge cases.**\n\n- **Two plugins both `deny`.** Both reasons accumulate via `additionalContext`? Or only the first wins? Docs don't specify. Low-impact since either outcome serves the user (they get a denial and at least one useful reason). Will empirically verify in v0.20.x integration tests.\n- **A plugin returns `ask` while we return `deny`.** By precedence, deny wins, user is not prompted. Intended behaviour — we don't want other plugins pulling users into permission dialogues we meant to silently handle.\n\n**Escalation.** If a real composition issue surfaces, we add a `hookPriority` config key and document install-order best practices. Not a v0.20.0 blocker — the platform precedence rules are sufficient for our design's correctness.\n\n**Testing requirement.** §17 adds one integration test: install token-pilot alongside a mock plugin that returns `allow`, run a deny-qualifying Read, assert our deny wins and our reason reaches the model.\n\n---\n\n## 13. Failure Modes\n\nEvery failure path must degrade to **pass-through, never abort**.\n\n| Failure | Handling |\n|---------|----------|\n| Stdin malformed JSON | exit 0, no output (pass-through). Stderr log. |\n| File missing / unreadable | exit 0, no output. |\n| Summary pipeline all fallbacks fail | exit 0, no output. Stderr log. |\n| ast-index subprocess fails | Fall through to regex parser; no user-visible error. |\n| `projectRoot` resolution fails | exit 0, no output. Stderr log once per session. |\n| Node crash mid-execution | Hook exits non-zero; Claude Code treats as advisory (documented fallback). |\n\nA **contract test** asserts each of these paths. Regressions here are release-blocking.\n\n---\n\n## 14. Migration Plan\n\n### 14.1 Version roadmap\n\n| Version | Default mode | New features | User action |\n|---------|--------------|--------------|-------------|\n| v0.19.x | `deny` (old) | — | None. |\n| **v0.20.0** | `advisory` (auto-migrated from old `deny`) | `deny-enhanced` mode opt-in. `SessionStart` hook opt-in. `bless-agents` CLI. **Tier 1 `tp-*` subagents** (tp-run, tp-onboard, tp-pr-reviewer, tp-impact-analyzer, tp-refactor-planner, tp-test-triage) shipped via `install-agents` CLI. | Auto-migration of `mode: \"deny\"` → `\"advisory\"` with stderr notice. Users run `npx token-pilot install-agents` once to enable the agent family (prompts for user/project scope). |\n| v0.20.x | unchanged | Bugfixes, benchmarks, docs. **Tier 2 `tp-*` subagents** ship agent-by-agent as telemetry justifies (see §5.4.7). On upgrade, `install-agents` re-run updates unchanged files automatically. | Re-run `npx token-pilot install-agents` to pull new Tier 2 agents (idempotent for unchanged files). |\n| **v0.21.0** | `deny-enhanced` | New default. `SessionStart` hook auto-registered. Optional: marketplace plugin submission so `/plugin install token-pilot` works as an alternative distribution path. | Users preferring minimal intervention set `mode: \"advisory\"`. |\n\n**Old `deny` removal note.** Because the old `deny` mode is removed entirely, a one-time auto-migration runs on v0.20 startup: configs with `mode: \"deny\"` are rewritten to `mode: \"advisory\"` with a stderr notice explaining the change. Alternative would be to rewrite to `deny-enhanced` automatically. We choose `advisory` (the more conservative choice) so users do not unexpectedly see new verbose output until they opt in.\n\n**Agent family rollout note.** Tier 1 `tp-*` agents are bundled inside the npm package under `dist/agents/` but are **not** auto-written to the user's filesystem. A single explicit CLI command — `npx token-pilot install-agents` — copies them to the user-chosen scope (`~/.claude/agents/` or `./.claude/agents/`). Scope is always the user's choice, mirroring Claude Code's `/plugin install` UX. At MCP startup, if `tp-*` agents are absent, the server logs one stderr reminder to run the command (silenceable via config or env var). No silent writes to the user's home directory.\n\nTier 2 agents ship agent-by-agent in v0.20.x. On each npm upgrade that introduces new Tier 2 agents, the stderr reminder resumes until the user re-runs `install-agents` to pull them in. Idempotence protects any hand-edited agent files.\n\n### 14.2 Communication\n\n- CHANGELOG entry with \"what changed, how to keep the old behaviour, why we changed it\", including a section on the new `tp-*` family and a one-line install command.\n- One-line message on first v0.20 start: *\"token-pilot v0.20 available. Try `npx token-pilot install-agents` for guaranteed-savings subagents, or `token-pilot config set mode=deny-enhanced` for the enhanced hook.\"*\n- README section \"Quick start\" updated with the single `install-agents` command, the scope prompt, and a note about session restart.\n- v0.21 release notes pinned to README and MCP manifest.\n\n### 14.3 Rollback path\n\nEvery new feature is keyed to a config flag or reversible CLI action.\n\n- `mode: \"advisory\"` restores pre-v0.20 behaviour (minus the old `deny` mode which is removed — `advisory` is the closest preserved equivalent).\n- `SessionStart` hook → disabled via `sessionStart.enabled = false`.\n- `bless-agents` → undone by `unbless-agents`.\n- `install-agents` → undone by `uninstall-agents` (removes only token-pilot's own `tp-*` files; never touches user agents outside the family).\n- Stderr reminder for `install-agents` → silenced via `agents.reminder = false` or `TOKEN_PILOT_NO_AGENT_REMINDER=1`.\n\n---\n\n## 15. Telemetry \u0026 Reporting\n\n### 15.1 Event log\n\nPer-session JSONL at `.token-pilot/hook-events.jsonl`:\n\n```json\n{\"ts\":\"2026-04-17T20:15:00Z\",\"session_id\":\"...\",\"agent_type\":\"Explore\",\"agent_id\":\"a1\",\"event\":\"deny_enhanced\",\"file\":\"src/index.ts\",\"lines\":541,\"estTokens\":4941,\"summaryTokens\":1427,\"savedTokens\":3514}\n```\n\n### 15.2 Rotation\n\n- File rotates when \u003e 10 MB (renamed to `hook-events.\u003ctimestamp\u003e.jsonl`).\n- Retention: 30 days (configurable).\n- Total cap: 100 MB; oldest deleted first.\n\n### 15.3 Commands\n\n- `token-pilot stats` — cumulative totals (reads processed, tokens saved).\n- `token-pilot stats --session` — current session only.\n- `token-pilot stats --by-agent` — breakdown by `agent_type`.\n- `token-pilot stats --since=7d` — time window.\n\n### 15.4 Exit report\n\nWhen `sessionStart.showStats: true`, the SessionStart hook reports prior-session totals. When a session ends (Stop hook, optional), emit a one-line summary to stderr.\n\n---\n\n## 16. `bless-agents` CLI — Detailed Spec\n\n### 16.1 Commands\n\n```\ntoken-pilot bless-agents [--auto|--interactive|--dry-run]\ntoken-pilot unbless-agents [--all|\u003cname\u003e...]\ntoken-pilot agents list\ntoken-pilot agents diff \u003cname\u003e    # show upstream vs blessed\ntoken-pilot doctor                # includes agent-drift check\n```\n\n### 16.2 Scan locations (precedence: highest first)\n\n1. `./.claude/agents/**/*.md`\n2. `~/.claude/agents/**/*.md`\n3. `${CLAUDE_PLUGIN_ROOT}/agents/**/*.md` (plugin-contributed)\n4. Built-in agents are **not** in the filesystem; they cannot be blessed — documented limitation.\n\n### 16.3 Built-in agents\n\nClaude Code built-ins (e.g. `general-purpose`, `Plan`) are defined by Claude Code itself, not by files we can read. Two paths:\n\n- **Most built-ins are Category A/B** (have `*` or `All tools except ...`) so they already have MCP access.\n- For any built-in in Category C, users must create a manual project-level override. `bless-agents` documents this in its summary output.\n\n### 16.4 Drift detection\n\n- On bless, store `upstream_hash = sha256(upstream file content)` in the blessed override's frontmatter.\n- `token-pilot doctor` walks all blessed agents and recomputes upstream hashes. Warns on mismatch with remediation: `token-pilot re-bless \u003cname\u003e`.\n\n### 16.5 Safety contract\n\n- Never modifies upstream agent files.\n- Never touches a project agent that lacks the `blessed: true` marker.\n- `--dry-run` shows the diff without writing.\n- Writes are atomic (temp file + rename).\n\n### 16.6 Discovery path for existing users (not first-install)\n\nMost v0.19 users already have token-pilot installed and will never run `init` again. For them, three discovery paths:\n\n1. **Version-change nudge.** `.token-pilot-fingerprint.json` already tracks the installed version. On first v0.20 run after upgrade, emit a one-time stderr notice: *\"token-pilot v0.20 adds `bless-agents` — run `npx token-pilot bless-agents` to extend savings to specialised subagents.\"* The notice is keyed to a `noticedVersions: [\"0.20.0\"]` entry in the fingerprint so it only fires once.\n2. **SessionStart nudge (opt-in).** When `sessionStart.showStats: true`, the session-start reminder appends *\"(14 subagents without MCP access detected — run `token-pilot bless-agents` to fix)\"* exactly once per session when there are unblessed Category-C agents present. Never nags.\n3. **CHANGELOG + README.** Standard release-notes path. Pinned entry at the top of the README.\n\nAll three paths are passive — the user always drives the actual `bless-agents` invocation.\n\n### 16.7 First-install discovery\n\nFor fresh installs (new user running `token-pilot init` for the first time in v0.20+):\n\n```\n$ token-pilot init\nInstalling token-pilot...\n✓ Hook registered\n✓ MCP config added\n\nScanning installed agents...\n  Found 14 subagents without MCP access.\n  These agents could use token-pilot via a one-command upgrade.\n  Run bless-agents now? [y/N]: _\n```\n\n- `y` → runs `bless-agents --auto`.\n- `N` → continues without blessing; user can always run `bless-agents` later.\n\nInteractive prompt is skipped when stdin is not a TTY (CI / automation).\n\n### 16.8 Claude Code session scope — newly-written agents require a session restart\n\n**Empirically verified (2026-04-17).** Claude Code loads the plugin agent registry at session start. Adding a new `.claude-plugin/agents/*.md` or a new project-level override in `.claude/agents/*.md` during a running session does **not** make the agent available to that session. Invoking the fresh name returns `Agent type '\u003cname\u003e' not found`.\n\n**Implications for every command that writes agent files:**\n\n- `token-pilot bless-agents` — after writing overrides, tell the user: *\"Blessed N agents. **Start a new Claude Code session** to pick them up.\"* Include this line in CLI output and `doctor`.\n- `token-pilot init` (when it proactively offers bless) — include the same notice.\n- `token-pilot unbless-agents` — same notice; removing files from a live session won't hide them either.\n- Tier 1 / Tier 2 `tp-*` rollout — on every npm upgrade of `token-pilot`, log a stderr line once: *\"token-pilot updated: new agents available. Start a new Claude Code session to see tp-\\\u003cname\\\u003e.\"*\n\n**Desktop app guidance** added to the user-facing docs:\n\n- **macOS / Windows app**: open a new conversation (the app's \"New Chat\" / `⌘N` equivalent) in the same project, or fully quit (`⌘Q`) and relaunch if new chat still does not show the agents.\n- **CLI `claude`**: exit the current session and start `claude` again.\n\n**Testing.** `token-pilot doctor --restart-hint` command returns a human-readable block the user can paste if confused.\n\n---\n\n## 17. Testing Strategy\n\n### 17.1 Unit tests\n\n- Hook classification (code ext / threshold / bounded).\n- Summary pipeline primary / fallback transitions.\n- Config loader precedence.\n- `bless-agents` scanning and classification.\n- Path safety (realpath, startsWith, traversal).\n\n### 17.2 Integration tests\n\n- Real hook invocation with stdin fixtures covering: main agent, subagent (agent_type set), bounded Read, non-code file, small file, missing file, malformed JSON.\n- `bless-agents` creating and undoing overrides against a fixture `.claude/agents/` tree.\n\n### 17.3 Golden / snapshot tests\n\n- Summary output for a canonical set of files (one per language) — changes require explicit approval.\n\n### 17.4 E2E\n\n- Harness that boots Claude Code in dry mode with token-pilot installed, runs a scripted agent session, asserts hook fired and token counts match expectations.\n- Composition test: install a fake secondary `PreToolUse:Read` hook alongside ours, verify behaviour.\n\n### 17.5 Performance benchmark\n\n- `npm run bench:hook` — measures p50 / p95 / p99 latency for the hook under cold and warm conditions. CI fails if p95 \u003e 150 ms.\n\n### 17.6 Empirical `tool_input` forwarding test\n\nMust pass before we consider adding `Read(path, {bypass: true})` as a fourth bypass path:\n\n**Test environment.** Claude Code with token-pilot installed. A debug hook that logs full `tool_input` to a file.\n\n**Test matrix.** For each of: main agent, a Category-A subagent (`general-purpose`), a Category-B subagent (`Explore`), and a Category-C subagent (an `acc-*` that has `tools:` restricted but includes `Read`):\n\n1. Agent calls `Read(path)` — baseline.\n2. Agent calls `Read(path, {bypass: true})` — does the extra key arrive in the hook's stdin? Does the Read tool reject it client-side before the hook sees it?\n3. Agent calls `Read(path, {frobnicate: 42})` — control for \"any unknown key\".\n\n**Pass criteria.** All three subagent categories must forward unknown keys through to the hook for this bypass path to be viable. If any category rejects client-side, we cannot rely on the flag and stick with the three paths in §5.1.3.\n\n**Parking.** Test is not a v0.20.0 blocker. It runs in v0.20.x. If results are positive, we add the flag as a non-breaking change in v0.21. If negative, we document and drop the idea.\n\n---\n\n## 18. Open Questions\n\n| # | Question | Default answer | Blocks ship? |\n|---|----------|----------------|--------------|\n| Q1 | Should the SessionStart reminder be a system reminder style block, or plain `additionalContext` text? | `additionalContext` text (safer, documented). | No |\n| Q2 | Should the deny message include historical stats (\"last session saved 72 %\")? | Yes in v0.21, off by default in v0.20 to keep payload short. | No |\n| Q3 | For Category-C built-ins we cannot bless, should we log a notice on first deny? | Log once per session, offer `bless-agents` command as remediation. | No |\n| Q4 | Should `read_full_file_expensive` be gated behind a config flag? | Yes — off by default; users enable per project. | No |\n| Q5 | For monorepos: one cache dir at the repo root, or one per package? | None in enhanced deny (no cache). Revisit if we add caching later. | No |\n| Q6 | Does Claude Code's Read tool forward unknown keys in `tool_input` (like `{bypass: true}`) to the hook? | Parked — test in v0.20.x per §17.6, accept only if all subagent categories pass. Meanwhile design has three bypass paths that do not depend on this. | No |\n| Q7 | How does SessionStart hook interact with subagent spawns? Does it fire once per top-level session or for subagent sub-sessions too? | Expected: top-level only. Will confirm during implementation. Not a blocker — Category-C subagents learn via the deny-enhanced message, which arrives at their Read call regardless. | No |\n| Q8 | Does the main agent actually pick `tp-*` agents when task-descriptions match, given our frontmatter `description:` fields, or does it default to built-ins? | Empirical test in v0.20.0-rc: run canonical fixtures (onboarding, PR review, impact, refactor, test triage) with both only-`tp-*` and only-built-ins available, compare selection rates. If main agent prefers built-ins, we tune `description:` copy. | No |\n| Q9 | Are `tp-*` agents invoked with the same `additionalContext` that the main agent received, or do they start with an empty context window? | Standard Claude Code behaviour: subagents get their own fresh context. Our SessionStart hint doesn't help them. Mitigation is built in: the base preamble of every `tp-*` system prompt carries the contract, so subagent context is self-sufficient. | No |\n| Q10 | Cold prompt length: shared preamble (~12 lines) + agent-specific block (~10–30 lines) + frontmatter. Is total ≤ 60 lines per agent, so we do not bloat the main agent's delegate listing? | Target ≤ 60 lines (description in frontmatter is what the main agent sees for routing; body is only loaded when agent is invoked). CI check on agent files. | No |\n| Q11 | What format do bundled agent templates use inside the npm package — raw `.md` static files in `dist/agents/`, or inlined TypeScript string constants? | Static `.md` files at `dist/agents/tp-*.md`, copied from source `templates/agents/` at build time. Enables external inspection, simpler diffs on upgrades, no escaping pain. | No |\n| Q12 | Handling of users who install both `user` and `project` scopes (e.g., global agents plus a project override): does `install-agents --scope=project` silently double the footprint? | Yes by design — Claude Code agent-precedence gives project \u003e user, so a project file overrides. `doctor` surfaces this explicitly so the user knows. | No |\n\n---\n\n## 19. Risks \u0026 Mitigations\n\n| Risk | Likelihood | Impact | Mitigation |\n|------|-----------|--------|------------|\n| Agent re-reads after seeing summary (\"I need to see the real file\") | Medium | Undermines savings | Summary explicitly tells agent to use bounded Read for specifics; telemetry tracks re-read rate; threshold tuned in v0.20.x. |\n| `additionalContext` token cost offsets savings | Low | Minor | SessionStart reminder capped at 250 tokens (incl. agent list); summary at 1200 tokens; both much smaller than typical large-file Read. |\n| Claude Code API changes `hookSpecificOutput` shape | Low | High | Integration tests against Claude Code fixtures; CI verifies parsing; gated fallback emits plain stderr. |\n| Hook composition issues with other plugins | Medium | Medium | E2E composition tests; documented install order; `hookPriority` escape hatch. |\n| Users complain about \"too wordy\" deny messages | Medium | Low | `advisory` mode always available; `deny-enhanced` summary is capped. Users who preferred old `deny` silence are auto-migrated to `advisory`. |\n| Agents reach for `read_full_file_expensive` casually | Low | Medium | Name designed to discourage; config-gated; telemetry tracks usage. |\n| Path-traversal in hook | Low | High | realpath + startsWith check with unit tests. |\n| Node cold-start latency perceptible to users | Medium | Low | Benchmark-gated; MCP IPC already warm; future daemon option. |\n| Main agent does not delegate to `tp-*` agents even when appropriate | Medium | Medium | SessionStart reminder lists them; frontmatter `description:` is task-focused; Q8 empirical test before v0.20.0 final. |\n| `tp-*` system prompt drift weakens the MCP-first discipline across releases | Low | High | Behaviour-drift CI test (§5.4.8): golden task re-run, fail if token usage rises \u003e 20 %. |\n| Maintenance burden of 11 agents grows unsustainable | Medium | Medium | Shared base preamble keeps per-agent body small; hard ceiling of 15 agents (§5.4.7); Tier 2 gated by real usage telemetry. |\n| Main agent picks `tp-run` for trivial tasks where general-purpose would suffice, adding latency | Low | Low | `tp-run` description: \"use when code-reads dominate the task\"; benchmark justifies. |\n| Community-authored agents collide with `tp-*` names | Low | Medium | We own the `tp-` prefix in documentation; if a project-level override of a `tp-*` exists without our blessed marker, `token-pilot doctor` warns. |\n| User never runs `install-agents` → never benefits from the `tp-*` family | Medium | Low | One-line stderr reminder on every MCP startup until installed; README quickstart leads with this command; telemetry tracks how many installs per npm-download. |\n| User runs `install-agents --scope=user` on a shared machine, polluting other user accounts | Low | Low | `~/.claude/agents/` is per-user by OS convention; multi-user machines are rare for devtools. Documented. |\n| Upgrade path bug: new npm version leaves stale `tp-*` files in user's agents dir | Medium | Medium | Hash-based idempotence (§5.4.5): on `install-agents`, compare installed hash vs bundled template hash; update if unchanged, skip if user-edited, warn. Integration test per upgrade. |\n\n---\n\n## 20. Glossary\n\n- **Advisory / Deny-enhanced:** hook modes. Advisory prints a hint; deny-enhanced blocks and returns the structural summary.\n- **Category A/B/C subagent:** by MCP access via `tools:` allowlist — A/B have it (`*` / `All tools except …`), C does not (explicit list without `mcp__token-pilot__*`).\n- **Bless:** project-level override of a third-party subagent granting it MCP access.\n- **Substitute (rejected):** would have swapped `file_path` to a cache path. See §4.\n- **Summary:** structural outline (imports, exports, symbols) from the summary pipeline.\n- **Bypass:** escape paths from deny-enhanced — bounded Read, `read_full_file_expensive`, env `TOKEN_PILOT_BYPASS=1`.\n- **`tp-*`:** token-pilot-native subagents we fully control (see §5.4).\n- **Tier 1 / Tier 2:** v0.20.0 must-ship vs v0.20.x telemetry-gated.\n- **Shared preamble:** ~12-line MCP-first contract reused across every `tp-*` agent.\n\n---\n\n## Appendix A — Empirical findings\n\n- Regex-based summary of `src/index.ts` (541 lines / ~4941 tokens) → 103 lines / ~1427 tokens (72 % reduction). Validates summary-size assumptions in §5.1 / §11.\n- `updatedInput` substitution is technically supported by Claude Code. Rejected for Edit/mental-model reasons (§4).\n- Empirical: plugin agents in `.claude-plugin/agents/` are not auto-loaded unless token-pilot is installed as a marketplace plugin. Drove §5.4.5 CLI-based distribution.\n- Empirical: adding a new agent file mid-session does not hot-reload — requires a new Claude Code session (§16.8).\n\n## Appendix B — Review Record\n\nExternal architectural review by `system-architect-reviewer` (2026-04-17): 3 show-stoppers + 7 serious concerns in the initial substitution design. All incorporated throughout (§4, §§10–13).","notes":"## v0.21 Release Checklist\n\nItems parked for v0.21 to keep v0.20.0 scope bounded. Tracked here so they survive v0.20.x iteration.\n\n### Platform defaults\n\n- [ ] Promote default mode from `advisory` to `deny-enhanced`. Auto-rewrite remaining `advisory` → `deny-enhanced` with stderr notice (same pattern as v0.20's `deny` → `advisory` auto-migration).\n- [ ] Auto-register `SessionStart` reminder hook at first MCP start (opt-in in v0.20, default v0.21). Respect `sessionStart.enabled = false`.\n- [ ] Re-evaluate `Read(path, {bypass: true})` flag from Q6 empirical test (§17.6). If all subagent categories forward unknown `tool_input` keys, add as 4th bypass path non-breaking.\n\n### Subagent family\n\n- [ ] Ship Tier 2 agents that passed telemetry bar in v0.20.x (§5.4.7). Gate per agent: fixture + golden test + real-world task-intent frequency without matching `tp-*`.\n- [ ] Review Tier 1 prompts against 6 months of real output-budget data; tighten/loosen budgets per §5.4.8 drift-alert history.\n- [ ] Ship `--format=json` machine-parseable output mode for every Tier 1 agent. Schema docs in README.\n\n### Distribution — marketplace submission\n\n- [ ] Move Tier 1+2 `.md` files into `.claude-plugin/agents/` in parallel to `dist/agents/` (single source, build-time sync).\n- [ ] Verify `.claude-plugin/plugin.json`, `marketplace.json`, `hooks/hooks.json` are current (absolute paths resolving on marketplace layout).\n- [ ] Open PR to `anthropics/claude-plugins-official` (or publish own marketplace if chosen).\n- [ ] Post-merge: add \"Install as Claude Code plugin\" section to README as the second canonical install option (npm path stays first).\n- [ ] CHANGELOG v0.21 pinned section documenting dual-install model.\n\n### Operational\n\n- [ ] Publish aggregated, anonymised, opt-in \"Savings Report\" from telemetry.\n- [ ] `token-pilot doctor --compare-installs` reports active install scope (npm vs marketplace) and any redundancy.\n- [ ] Deprecation notice for any legacy v0.19 config keys now unreachable.\n\n### Non-goals for v0.21 (further parked)\n\n- Hook daemon for sub-30 ms latency (§11). Revisit only if benchmarks justify.\n- Per-session cache beyond `hook-events.jsonl`.\n- Forking built-in Claude Code agents for deeper integration.","status":"closed","priority":1,"issue_type":"feature","owner":"shahinyanm@gmail.com","created_at":"2026-04-17T17:03:59Z","created_by":"shahinyanm","updated_at":"2026-04-18T06:48:42Z","closed_at":"2026-04-18T06:48:42Z","close_reason":"v0.20.0 released. Branch claude/sweet-wilson-5ab84c pushed; tag v0.20.0 created; GitHub release https://github.com/Digital-Threads/token-pilot/releases/tag/v0.20.0 published. npm publish pending human decision. TP-qzj and TP-yk9 (discussion issues) remain for v0.21 planning. Deferred to v0.20.x: live-LLM behavioural assertions + tp-run vs general-purpose ratio + hook-daemon+CI gate for bench thresholds — tracked in TP-m43 notes before close.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"TP-c08","title":"doctor: detect Claude Code env vars from community guide","description":"The Claude usage-limit community guide (captured in TP-yk9) names four environment variables that give 60-80 percent session savings with zero effort: CLAUDE_CODE_SUBAGENT_MODEL=haiku (routes subagents to Haiku, much cheaper), MAX_THINKING_TOKENS=10000 (caps hidden reasoning tokens), CLAUDE_AUTOCOMPACT_PCT_OVERRIDE=50 (compacts at 50 percent instead of 95 percent), model=sonnet in ~/.claude/settings.json (default Sonnet instead of Opus). token-pilot doctor should read ~/.claude/settings.json + current env and show a one-line tip per missing knob with the exact line to add. Never modify user settings — pure advisory.","status":"closed","priority":2,"issue_type":"task","owner":"shahinyanm@gmail.com","created_at":"2026-04-18T07:55:50Z","created_by":"shahinyanm","updated_at":"2026-04-18T08:15:02Z","closed_at":"2026-04-18T08:15:02Z","close_reason":"Shipped in v0.21.0. TP-c08: doctor advisor surfaces CLAUDE_CODE_SUBAGENT_MODEL/MAX_THINKING_TOKENS/CLAUDE_AUTOCOMPACT_PCT_OVERRIDE/model=sonnet tips. TP-rtg: .claudeignore generator (managed-marker, non-destructive) + CLAUDE.md hygiene check in doctor. TP-jzh: re-framed as Bash output advisor after Context7 confirmed PostToolUse cannot truncate tool_response for non-MCP Bash tool — now a PostToolUse:Bash hook that appends a one-line tip when output exceeds ~8000 chars. 843 tests, tsc clean. Release https://github.com/Digital-Threads/token-pilot/releases/tag/v0.21.0","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"TP-q33","title":"Enforce per-agent response budgets + live behavioural test","description":"Today tp-* agents get a budget line in the preamble but nothing enforces it — if an agent writes 1200 tokens with a 400 budget, no alarm. Two parts: (a) Add a token-counter check to response returned from subagents (where feasible in Claude Code API); if over budget by more than 10 percent, log to .token-pilot/over-budget.log and emit in stats. (b) Live behavioural harness gated on ANTHROPIC_API_KEY — runs each tp-* agent against tests/fixtures/golden-repo, asserts MCP-first tool ordering, response size within budget+10 percent, no narration phrases, verdict-first format. Deferred from Phase 4 and 7. Proves our claim 'tp-run is 60 percent cheaper than general-purpose' with numbers.","status":"open","priority":2,"issue_type":"feature","owner":"shahinyanm@gmail.com","created_at":"2026-04-18T07:55:45Z","created_by":"shahinyanm","updated_at":"2026-04-18T07:55:45Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"TP-hsz","title":"Agent self-regulation tools: budget MCP + time-to-compact + project_overview delta + auto-suggest context-mode","description":"Give agents signals they can use to shape their own responses. (a) New MCP tool mcp__token-pilot__budget() — returns current session token usage, denyThreshold, estimated remaining room before /compact (from hook-events.jsonl burn rate). Agents like tp-pr-reviewer can use this to decide detailed vs terse output. (b) token-pilot stats --predict shows time-to-compact. (c) project_overview called twice in the same session returns only changed sections, not full rebuild. (d) Read hook on .log/.json files \u003e 1MB emits a hint 'use mcp__context-mode__execute_file instead' alongside the summary — cross-tool handoff.","status":"closed","priority":2,"issue_type":"feature","owner":"shahinyanm@gmail.com","created_at":"2026-04-18T07:55:39Z","created_by":"shahinyanm","updated_at":"2026-04-18T09:58:13Z","closed_at":"2026-04-18T09:58:13Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"TP-yk9","title":"Discuss: community usage-limit workarounds — what we can absorb into token-pilot","description":"User shared a comprehensive community guide (2026-03-30) on Claude usage-limit workarounds. Many items overlap with token-pilot's scope (subagent model routing, .claudeignore, CLAUDE.md hygiene, read-once, monitoring, PostToolUse truncation). Post-Phase 1 discussion to decide which of these to adopt as features for v0.21+. Full text in --notes.","notes":"Reference: Comprehensive Workaround Guide for Claude Usage Limits (community, Updated 2026-03-30).\n\nUser shared this during Phase 1 implementation on 2026-04-18. Keep for design inspection — many items overlap with token-pilot scope (subagent model routing, context compaction, read-once, CLAUDE.md hygiene, monitoring). Post-Phase 1 discussion item.\n\nKey: 🌐 = claude.ai web/mobile/desktop app | 💻 = Claude Code CLI | 🔑 = API\n\nTHE PROBLEM IN BRIEF\nAnthropic silently introduced peak-hour multipliers (~March 23-26) that make session limits burn faster during US business hours (5am-11am PT). This was preceded by a 2x off-peak promo (March 13-28) that many now see as a bait-and-switch. On top of the intentional changes, there appear to be genuine bugs — users reporting 30-100% of session limits consumed by a single prompt, usage meters jumping with no prompt sent, and sessions starting at 57% before any activity. Affects all tiers from Free to Max 20x ($200/mo). Anthropic claims ~7% of users affected; community consensus is it's the majority of paying users.\n\nA. WORKAROUNDS FOR EVERYONE (Web App, Mobile, Desktop, Code CLI)\nThese require no special tools. Work on all plans including Free.\n\nA1. Switch from Opus to Sonnet 🌐💻🔑 — All Plans\nThis is the single biggest lever for web/app users. Opus 4.6 consumes roughly 5x more tokens than Sonnet for the same task. Sonnet handles ~80% of tasks adequately. Only use Opus when you genuinely need superior reasoning.\n\nA2. Switch from the 1M context model back to 200K 🌐💻 — All Plans\nAnthropic recently changed the default to the 1M-token context variant. Most people didn't notice. This means every prompt sends a much larger payload. If you see \"1M\" or \"extended\" in your model name, switch back to standard 200K. Multiple users report immediate improvement.\n\nA3. Start new conversations frequently 🌐 — All Plans\nIn the web/mobile app, context accumulates with every message. Long threads get expensive. Start a new conversation per task. Copy key conclusions into the first message if you need continuity.\n\nA4. Be specific in prompts 🌐💻 — All Plans\nVague prompts trigger broad exploration. \"Fix the JWT validation in src/auth/validate.ts line 42\" is up to 10x cheaper than \"fix the auth bug.\" Same for non-coding: \"Summarize financial risks in section 3 of the PDF\" vs \"tell me about this document.\"\n\nA5. Batch requests into fewer prompts 🌐💻 — All Plans\nEach prompt carries context overhead. One detailed prompt with 3 asks burns fewer tokens than 3 separate follow-ups.\n\nA6. Pre-process documents externally 🌐💻 — All Plans, especially Pro/Free\nConvert PDFs to plain text before uploading. Parse documents through ChatGPT first (more generous limits) and send extracted text to Claude. Pro users doing research report PDFs consuming 80% of a session — this helps a lot.\n\nA7. Shift heavy work to off-peak hours 🌐💻 — All Plans\nOutside weekdays 5am-11am PT. Caveat: many users report being hit hard outside peak hours too since ~March 28. Officially recommended by Anthropic but not consistently reliable.\n\nA8. Session timing trick 🌐💻 — All Plans\nYour 5-hour window starts with your first message. Start it 2-3 hours before real work. Send any prompt at 6am, start real work at 9am. Window resets at 11am mid-focus-block with fresh allocation.\n\nB. CLAUDE CODE CLI WORKAROUNDS\n⚠️ These ONLY work in Claude Code (terminal CLI). NOT in the web app, mobile app, or desktop app.\n\nB1. The settings.json block — DO THIS FIRST 💻 — Pro, Max 5x, Max 20x\nAdd to ~/.claude/settings.json:\n  {\n    \"model\": \"sonnet\",\n    \"env\": {\n      \"MAX_THINKING_TOKENS\": \"10000\",\n      \"CLAUDE_AUTOCOMPACT_PCT_OVERRIDE\": \"50\",\n      \"CLAUDE_CODE_SUBAGENT_MODEL\": \"haiku\"\n    }\n  }\nWhat this does: defaults to Sonnet (~60% cheaper), caps hidden thinking tokens from 32K to 10K (~70% saving), compacts context at 50% instead of 95% (healthier sessions), and routes all subagents to Haiku (~80% cheaper). This single config change can cut consumption 60-80%.\n\nB2. Create a .claudeignore file 💻 — Pro, Max 5x, Max 20x\nWorks like .gitignore. Stops Claude from reading node_modules/, dist/, *.lock, __pycache__/, etc. Savings compound on every prompt.\n\nB3. Keep CLAUDE.md under 60 lines 💻 — Pro, Max 5x, Max 20x\nThis file loads into every message. Use 4 small files (~800 tokens total) instead of one big one (~11,000 tokens). That's a 90% reduction in session-start cost. Put everything else in docs/ and let Claude load on demand.\n\nB4. Install the read-once hook 💻 — Pro, Max 5x, Max 20x\nClaude re-reads files way more than you'd think. This hook blocks redundant re-reads, cutting 40-90% of Read tool token usage. One-liner install:\n  curl -fsSL https://raw.githubusercontent.com/Bande-a-Bonnot/Boucle-framework/main/tools/read-once/install.sh | bash\nMeasured: ~38K tokens saved on ~94K total reads in a single session.\n\nB5. /clear and /compact aggressively 💻 — Pro, Max 5x, Max 20x\n/clear between unrelated tasks (use /rename first so you can /resume). /compact at logical breakpoints. Never let context exceed ~200K even though 1M is available.\n\nB6. Plan in Opus, implement in Sonnet 💻 — Max 5x, Max 20x\nUse Opus for architecture/planning, then switch to Sonnet for code gen. Opus quality where it matters, Sonnet rates for everything else.\n\nB7. Install monitoring tools 💻 — Pro, Max 5x, Max 20x\nAnthropic gives you almost zero visibility. These fill the gap:\n  npx ccusage@latest — token usage from local logs, daily/session/5hr window reports\n  ccburn --compact — visual burn-up charts, shows if you'll hit 100% before reset. Can feed ccburn --json to Claude so it self-regulates\n  Claude-Code-Usage-Monitor — real-time terminal dashboard with burn rate and predictive warnings\n  ccstatusline / claude-powerline — token usage in your status bar\n\nB8. Save explanations locally 💻 — Pro, Max 5x, Max 20x\n  claude \"explain the database schema\" \u003e docs/schema-explanation.md\nReferencing this file later costs far fewer tokens than re-analysis.\n\nB9. Advanced: Context engines, LSP, hooks 💻 — Max 5x, Max 20x (setup cost too high for Pro budgets)\n  Local MCP context server with tree-sitter AST — benchmarked at -90% tool calls, -58% cost per task\n  LSP + ast-grep as priority tools in CLAUDE.md — structured code intelligence instead of brute-force traversal\n  claude-warden hooks framework — read compression, output truncation, token accounting\n  Progressive skill loading — domain knowledge on demand, not at startup. ~15K tokens/session recovered\n  Subagent model routing — explicit model: haiku on exploration subagents, model: opus only for architecture\n  Truncate command output in PostToolUse hooks via head/tail\n\nC. ALTERNATIVE TOOLS \u0026 MULTI-PROVIDER STRATEGIES\nThese work for everyone regardless of product or plan.\n\n  Codex CLI ($20/mo) — Most cited alternative. GPT 5.4 competitive for coding. Open source. Many report never hitting limits. Caveat: OpenAI may impose similar limits after their own promo ends.\n  Gemini CLI (Free) — 60 req/min, 1,000 req/day, 1M context. Strongest free terminal alternative.\n  Gemini web / NotebookLM (Free) — Good fallback for research and document analysis when Claude limits are exhausted.\n  Cursor (Paid) — Sonnet 4.6 as backend reportedly offers much more runtime. One user ran it 8 hours straight.\n  Chinese open-weight models (Qwen 3.6, DeepSeek) — Qwen 3.6 preview on OpenRouter approaching Opus quality. Local inference improving fast.\n\nHybrid workflow (MOST SUSTAINABLE):\n  Planning/architecture → Claude (Opus when needed)\n  Code implementation → Codex, Cursor, or local models\n  File exploration/testing → Haiku subagents or local models\n  Document parsing → ChatGPT (more generous limits)\n  Research → Gemini free tier or Perplexity\n\nAPI direct (Pay-per-token) — Predictable pricing with no opaque multipliers. Cached tokens don't count toward limits. Batch API at 50% pricing for non-urgent work.\n\nTHE UNCOMFORTABLE TRUTH\nIf you're a claude.ai web/app user (not Claude Code), your options are essentially Section A above — which mostly boils down to \"use less\" and \"use it differently.\" The powerful optimizations (hooks, monitoring, context engines) are all CLI-only.\nIf you're on Pro ($20), the Reddit consensus is brutal: the plan is barely distinguishable from Free right now. The workarounds help marginally.\nIf you're on Max 5x/20x with Claude Code, the settings.json block + read-once hook + lean CLAUDE.md + monitoring tools can stretch your usage 3-5x further. Which means the limits may be tolerable for optimized setups — but punishing for anyone running defaults, which is most people.\n\nThe community is also asking Anthropic for: a real-time usage dashboard, published stable tier definitions, email comms for service changes, a \"limp home mode\" that slows rather than hard-cuts, and limit resets for the silent A/B testing period.\n\n---\n\nWHERE THIS INTERSECTS WITH TOKEN-PILOT (for post-Phase 1 discussion):\n- B1 subagent routing: our SessionStart reminder + tp-* family already covers this philosophy. Could add a config hint: recommend `CLAUDE_CODE_SUBAGENT_MODEL=haiku` for tp-* agents without system-prompt override.\n- B2 .claudeignore: we already have `ignore[]` in TokenPilotConfig. Could advise / generate a .claudeignore on `token-pilot init`.\n- B3 CLAUDE.md hygiene: could emit a warning/doctor check if project CLAUDE.md \u003e 60 lines.\n- B4 read-once hook: conceptually similar to our deny-enhanced — we dedupe by structure, they dedupe by identity. Could integrate: remember \"this file has been read in this session\" and skip re-summary, just refer back.\n- B5 /clear aggressive: aligns with our SessionStart reminder design.\n- B7 monitoring: our hook-events.jsonl + `token-pilot stats` mirror ccusage/ccburn. Could produce a similar status-bar component.\n- B9 progressive skill loading: the skill files we ship (guide, install, stats) are already on-demand. Worth verifying nothing loads at session-start unprompted.\n- B9 PostToolUse truncation: we could add a Bash-output truncator hook to the suite.","status":"closed","priority":2,"issue_type":"task","owner":"shahinyanm@gmail.com","created_at":"2026-04-17T20:07:59Z","created_by":"shahinyanm","updated_at":"2026-04-18T07:57:16Z","closed_at":"2026-04-18T07:57:16Z","close_reason":"Filtered through cost/benefit lens against v0.20 surface. 12 actionable items created under TP-gsk (v0.21 epic): 5 must-have (session dedup, .claudeignore+CLAUDE.md, bash truncator, Tier 2 agents, marketplace), 3 should-have (self-regulation tools, live behavioural test + hard cap, doctor env check), 4 nice-to-have (adaptive threshold, session continuity, save-doc, hook daemon). Filtered out: UX-only polish without token impact (dry-run, config wizard, first-run guide), external ecosystem pieces (Codex/Gemini fallback, ChatGPT preprocessing), and items already covered in v0.20 (subagent routing, progressive skills, monitoring baseline). Discussion closed; execution lives in TP-gsk subgraph.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"TP-qzj","title":"Discuss: ast-index + context-mode + token-pilot MCP — interaction model","description":"Open discussion item for end of Phase 1 (or later). Three related tools: (1) ast-index binary from github.com/defendend/Claude-ast-index-search — structural code indexer used by our hook summary pipeline. (2) context-mode MCP (mksglu/claude-context-mode) — sandbox execution of code, only stdout enters context. (3) token-pilot's own MCP functions — smart_read, read_symbol, etc. Question: should they be recommended as a composable toolkit? Which is primary for which user intent? Do we want to cross-reference them in our deny-enhanced message footer or CLAUDE.md template? User flagged this to discuss after implementation settles.","status":"closed","priority":2,"issue_type":"task","owner":"shahinyanm@gmail.com","created_at":"2026-04-17T19:27:41Z","created_by":"shahinyanm","updated_at":"2026-04-18T09:57:50Z","closed_at":"2026-04-18T09:57:50Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"TP-bot","title":"session_snapshot tool — автосжатие состояния сессии","description":"ChatGPT ставит state snapshots на 1-е место по эффекту экономии токенов. Нужен MCP tool session_snapshot, который автоматически собирает: цель, что проверили, текущая гипотеза, релевантные файлы, next step. Вызывается каждые N шагов или вручную. Выход — компактный блок \u003c200 токенов вместо полной истории.","status":"closed","priority":2,"issue_type":"feature","assignee":"shahinyanm","owner":"shahinyanm@gmail.com","created_at":"2026-04-15T05:05:31Z","created_by":"shahinyanm","updated_at":"2026-04-15T05:19:25Z","closed_at":"2026-04-15T05:19:25Z","close_reason":"Implemented and tested","dependencies":[{"issue_id":"TP-bot","depends_on_id":"TP-b5q","type":"child-of","created_at":"2026-04-15T09:06:00Z","created_by":"shahinyanm","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"TP-b5q","title":"Roadmap из анализа ChatGPT: расширение token-pilot","description":"ChatGPT проанализировал стратегии экономии токенов и поставил token-pilot в топ-2 по реальному эффекту. Выявлены 3 gap-а: state snapshots, context budget, session compaction. Плюс маркетинговые выводы.","status":"closed","priority":2,"issue_type":"epic","owner":"shahinyanm@gmail.com","created_at":"2026-04-15T05:05:16Z","created_by":"shahinyanm","updated_at":"2026-04-15T05:45:29Z","closed_at":"2026-04-15T05:45:29Z","close_reason":"All 4 child tasks completed: session_snapshot, context_budget, session_compaction, marketing","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"TP-z64","title":"Combo agents — expand tp-* roster with tool-combination specialists for v0.28","description":"User-flagged for v0.28: design and ship additional tp-* subagents that each pair novel combinations of existing MCP tools for niche workflows. Examples to brainstorm before implementation: (a) tp-review-impact = smart_diff + find_usages + impact-analyzer output, for 'will this PR break production?', (b) tp-test-coverage-gapper = find_unused + test_summary + outline, for 'which symbols have zero test coverage', (c) tp-api-surface-tracker = outline + find_usages + smart_log, for 'what changed in our exported surface over last N commits', (d) tp-dep-health = module_info + smart_log + find_usages, for 'which dependencies are stale and risky', (e) tp-incident-timeline = smart_log + smart_diff + find_usages, for 'what changed in the N hours before the bug'. Each proposed agent must have a tight budget (≤600 tokens), clear PROACTIVELY trigger, and demonstrable token savings vs doing the workflow main-thread. Do NOT start before v0.24 onboarding wizard ships. Brainstorm with user first — names, triggers, tool combinations.","status":"closed","priority":3,"issue_type":"feature","owner":"shahinyanm@gmail.com","created_at":"2026-04-18T11:40:34Z","created_by":"shahinyanm","updated_at":"2026-04-18T11:46:56Z","closed_at":"2026-04-18T11:46:56Z","close_reason":"Shipped as v0.24.0 — 5 combo agents: tp-review-impact, tp-test-coverage-gapper, tp-api-surface-tracker, tp-dep-health, tp-incident-timeline","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"TP-7i3","title":"Hook daemon — drop per-call Node cold start (~150ms → \u003c10ms)","description":"TP-816 section 11 identified this as future optimisation. Persistent local process, one-per-project, listens on a Unix socket. Claude Code hook command becomes a thin client that writes to the socket and reads one-line reply. First call spins up the daemon, subsequent calls serve warm. Retains full compatibility with existing hook-read/hook-edit/hook-session-start dispatch. When daemon is running, bench-hook thresholds in TP-816 section 11 (warm p50 \u003c30ms, p95 \u003c100ms) become reachable and --check=true can become the default CI gate. No behavioural change for users beyond speed — keep the stateless fallback path intact for environments where spawning a daemon is problematic.","status":"open","priority":3,"issue_type":"feature","owner":"shahinyanm@gmail.com","created_at":"2026-04-18T07:56:41Z","created_by":"shahinyanm","updated_at":"2026-04-18T07:56:41Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"TP-89n","title":"save-doc helper — persist explanations to docs/ to avoid re-analysis","description":"New CLI: npx token-pilot save-doc \u003cprompt\u003e [--path=docs/\u003cname\u003e.md]. Behaviour: reads stdin or prompt arg, writes a note with header 'saved by token-pilot save-doc at ts' so the agent recognises it as persisted knowledge on next session. The token-saving mechanism is behavioural: once a topic is in docs/, subsequent agents prefer read_symbol on that doc instead of re-deriving. Pairs with B8 from community guide. Simple wrapper — real intelligence is agents knowing to check docs/ first.","status":"closed","priority":3,"issue_type":"task","owner":"shahinyanm@gmail.com","created_at":"2026-04-18T07:56:34Z","created_by":"shahinyanm","updated_at":"2026-04-18T09:58:23Z","closed_at":"2026-04-18T09:58:23Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"TP-340","title":"Session continuity: auto-snapshot before long Bash + auto-restore on SessionStart","description":"Two symmetric mechanics. (a) Before any Bash tool call that historically runs \u003e 1 min (npm test, cargo build, docker build — detected via command allowlist), automatically call session_snapshot and save to .token-pilot/last-snapshot.md. Protects against accidental session drop during long operations. (b) At SessionStart, if .token-pilot/last-snapshot.md exists AND is less than 1 day old AND no tp-* session-restorer was already invoked, append a one-line reference ('previous session snapshot at ..., call tp-session-restorer to pick up where you left off') to the reminder.","status":"closed","priority":3,"issue_type":"feature","owner":"shahinyanm@gmail.com","created_at":"2026-04-18T07:56:28Z","created_by":"shahinyanm","updated_at":"2026-04-18T09:58:29Z","closed_at":"2026-04-18T09:58:29Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"TP-bbo","title":"Adaptive threshold — hook gets stricter as session burns","description":"Right now hooks.denyThreshold is a constant (default 300 lines). Sessions near /compact budget should be more aggressive. Read hook reads total savedTokens for current session_id from hook-events.jsonl; if burned \u003e N percent of typical session budget, lower effective threshold (e.g. 300 -\u003e 150 at 70 percent burn, 300 -\u003e 80 at 90 percent). Fully opt-in via config.hooks.adaptiveThreshold=true. Zero regression risk for users who want constant behaviour.","status":"closed","priority":3,"issue_type":"feature","owner":"shahinyanm@gmail.com","created_at":"2026-04-18T07:56:21Z","created_by":"shahinyanm","updated_at":"2026-04-18T09:58:18Z","closed_at":"2026-04-18T09:58:18Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"TP-yxw","title":"Маркетинг: использовать валидацию ChatGPT в README и позиционировании","description":"ChatGPT независимо поставил symbol-first access в топ-2 стратегий экономии токенов. Это сильный social proof. Задачи: 1) Добавить в README секцию 'Why this approach works' со ссылкой на принцип. 2) Структура '3 уровня оптимизации' из отзыва хорошо ложится на landing. 3) Конкретная метрика ~83% уже есть — вынести на видное место.","notes":"Часть эпика TP-b5q","status":"closed","priority":3,"issue_type":"task","assignee":"shahinyanm","owner":"shahinyanm@gmail.com","created_at":"2026-04-15T05:05:46Z","created_by":"shahinyanm","updated_at":"2026-04-15T05:45:24Z","closed_at":"2026-04-15T05:45:24Z","close_reason":"Updated README with Why This Approach Works section, session_snapshot in tools, max_tokens docs, and MCP instructions","dependencies":[{"issue_id":"TP-yxw","depends_on_id":"TP-b5q","type":"child-of","created_at":"2026-04-15T09:06:34Z","created_by":"shahinyanm","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"TP-ame","title":"session_compaction — пересборка контекста каждые N шагов","description":"Из анализа ChatGPT: в долгих сессиях основной расход — повторная отправка старой истории. Нужен механизм, который каждые 5-10 tool calls предлагает compact: сохранить state snapshot, очистить accumulated context, продолжить с минимальным состоянием. Интеграция с Claude Code compaction hooks.","notes":"Часть эпика TP-b5q","status":"closed","priority":3,"issue_type":"feature","assignee":"shahinyanm","owner":"shahinyanm@gmail.com","created_at":"2026-04-15T05:05:41Z","created_by":"shahinyanm","updated_at":"2026-04-15T05:40:15Z","closed_at":"2026-04-15T05:40:15Z","close_reason":"Implemented compaction advisory via policy engine with configurable thresholds","dependencies":[{"issue_id":"TP-ame","depends_on_id":"TP-b5q","type":"child-of","created_at":"2026-04-15T09:06:29Z","created_by":"shahinyanm","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"TP-kc9","title":"context_budget — лимит токенов на шаг с auto-summary","description":"Идея из анализа ChatGPT: если smart_read возвращает слишком много, сначала отдать summary, потом точечно подгрузить нужное. Реализация: параметр max_tokens в smart_read/smart_read_many. При превышении — автоматический переход в outline+summary режим с возможностью drill-down.","notes":"Часть эпика TP-b5q","status":"closed","priority":3,"issue_type":"feature","assignee":"shahinyanm","owner":"shahinyanm@gmail.com","created_at":"2026-04-15T05:05:36Z","created_by":"shahinyanm","updated_at":"2026-04-15T05:37:19Z","closed_at":"2026-04-15T05:37:19Z","close_reason":"Implemented max_tokens in smart_read and smart_read_many with 3-level downgrade","dependencies":[{"issue_id":"TP-kc9","depends_on_id":"TP-b5q","type":"child-of","created_at":"2026-04-15T09:06:24Z","created_by":"shahinyanm","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
