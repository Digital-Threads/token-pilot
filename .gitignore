# Dependencies
node_modules/

# Build output
dist/
coverage/

# Note: composed tp-* agents live at repo-root ./agents/ (Claude Code plugin
# convention). They are BUILT artifacts — regenerated by `npm run build`
# (scripts/build-agents.mjs) — but must be versioned so `claude plugin install`
# reads them without requiring a build step first.

# OS files
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Environment
.env
.env.*

# Token Pilot managed binaries
.token-pilot/

# Internal docs (RND/SDD)
docs/rnd.md
docs/sdd.md
docs/code-review-report.md

# MCP Registry auth tokens
.mcpregistry_*

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
